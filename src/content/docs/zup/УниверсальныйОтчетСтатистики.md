---
title: Универсальный отчет статистики
slug: zup/универсальный-отчет-статистики
---


## ПолучитьПредставлениеНуляИзСтрокиФормата
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПредставлениеНуляИзСтрокиФормата(Знач СтрокаФормата) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьПредставлениеНуляИзСтрокиФормата(СтрокаФормата));
```
</details>

## ЗначениеОбластиПоАдресуИзСсылки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗначениеОбластиПоАдресуИзСсылки(КонтекстФормы, СтрСсылка, ИндексЯчейки = 1, ИмяЯчейкиФильтра = "", НомерРаздела = "", НомерСтроки = "", ИмяКолонки = ""); Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ЗначениеОбластиПоАдресуИзСсылки(КонтекстФормы, СтрСсылка, ИндексЯчейки, ИмяЯчейкиФильтра, НомерРаздела, НомерСтроки, ИмяКолонки);
```
</details>

## ИдентификаторЭлементаСпискаФормПоПредставлениюУниверсальногоОтчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИдентификаторЭлементаСпискаФормПоПредставлениюУниверсальногоОтчета(КонтекстФормы, ПредставлениеЭлемента); Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ИдентификаторЭлементаСпискаФормПоПредставлениюУниверсальногоОтчета(КонтекстФормы, ПредставлениеЭлемента););
```
</details>

## СохранитьДанныеРаздела
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СохранитьДанныеРаздела(КонтекстФормы, НаимРаздела) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.СохранитьДанныеРаздела(КонтекстФормы, НаимРаздела));
```
</details>

## УстановитьПредставлениеКлючевыхЯчеек
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьПредставлениеКлючевыхЯчеек(КонтекстФормы, СтруктураОпцийСтроки, ИндексСтроки, ТабДок = Неопределено) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.УстановитьПредставлениеКлючевыхЯчеек(КонтекстФормы, СтруктураОпцийСтроки, ИндексСтроки, ТабДок);
```
</details>

## ПрочитатьСекциюTitle
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПрочитатьСекциюTitle(ФайлОбмена, ПараметрыTitle) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ПрочитатьСекциюTitle(ФайлОбмена, ПараметрыTitle));
```
</details>

## ПрочитатьСекциюReport
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПрочитатьСекциюReport(КонтекстФормы, ФайлОбмена, ПараметрыReport) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ПрочитатьСекциюReport(КонтекстФормы, ФайлОбмена, ПараметрыReport));
```
</details>

## ДобавитьКолонкуВМассив
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьКолонкуВМассив(ФайлОбмена, МассивКолонок, СчКонецЭлемента) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ДобавитьКолонкуВМассив(ФайлОбмена, МассивКолонок, СчКонецЭлемента));
```
</details>

## ПрочитатьRow
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПрочитатьRow(ФайлОбмена, Результаты, СчКонецЭлемента) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ПрочитатьRow(ФайлОбмена, Результаты, СчКонецЭлемента));
```
</details>

## ПрочитатьСекциюSection
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПрочитатьСекциюSection(ФайлОбмена, Результаты) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПрочитатьСекциюSection(ФайлОбмена, Результаты));
```
</details>

## ПрочитатьСекциюSections
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПрочитатьСекциюSections(ФайлОбмена, ПараметрыSections) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ПрочитатьСекциюSections(ФайлОбмена, ПараметрыSections));
```
</details>

## УбратьЛидирующиеНули
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция УбратьЛидирующиеНули(Строка) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.УбратьЛидирующиеНули(Строка));
```
</details>

## ЗаменитьПредставлениеНуляВСтрокеФормата
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗаменитьПредставлениеНуляВСтрокеФормата(Знач СтрокаФормата, ПредставлениеНуля) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ЗаменитьПредставлениеНуляВСтрокеФормата(СтрокаФормата, ПредставлениеНуля));
```
</details>

## ИмяПервойЯчейкиОбласти
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИмяПервойЯчейкиОбласти(КонтекстФормы, ОбластьДокумента) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ИмяПервойЯчейкиОбласти(КонтекстФормы, ОбластьДокумента));
```
</details>

## СпециальногоУдаленияСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СпециальногоУдаленияСтроки(КонтекстФормы, ТекТабличноеПоле, НомерВерхЯчейкиОбласти, НомерНижнЯчейкиОбласти, НомерУдаляемойСтроки, КолСтрокРаздела) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.СпециальногоУдаленияСтроки(КонтекстФормы, ТекТабличноеПоле, НомерВерхЯчейкиОбласти, НомерНижнЯчейкиОбласти, НомерУдаляемойСтроки, КолСтрокРаздела));
```
</details>

## УдалитьДополнительнуюСтроку
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УдалитьДополнительнуюСтроку(КонтекстФормы, ИмяПервойЯчейкиСтроки) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.УдалитьДополнительнуюСтроку(КонтекстФормы, ИмяПервойЯчейкиСтроки));
```
</details>

## ОчиститьТабличноеПоле
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОчиститьТабличноеПоле(КонтекстФормы, ВыбТабличноеПолеИмя) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ОчиститьТабличноеПоле(КонтекстФормы, ВыбТабличноеПолеИмя));
```
</details>

## ОчиститьОбласть
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОчиститьОбласть(КонтекстФормы, ИмяСтраницы, ИмяОбласти) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ОчиститьОбласть(КонтекстФормы, ИмяСтраницы, ИмяОбласти));
```
</details>

## Расчет
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура Расчет(КонтекстФормы, ИмяТекущейСтраницы = "", ОбластьИмя = "") Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.Расчет(КонтекстФормы, ИмяТекущейСтраницы, ОбластьИмя);
```
</details>

## ОчиститьСекцию
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОчиститьСекцию(КонтекстФормы, Код) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ОчиститьСекцию(КонтекстФормы, Код));
```
</details>

## ВывестиМакетВТаблДокНаСервере
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВывестиМакетВТаблДокНаСервере(КонтекстФормы, ИмяТекРаздела, НомерСтраницыРазделаОтчета, ИдЭлементаФормыРаздела = Неопределено) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ВывестиМакетВТаблДокНаСервере(КонтекстФормы, ИмяТекРаздела, НомерСтраницыРазделаОтчета, ИдЭлементаФормыРаздела);
```
</details>

## ПолучитьКодСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьКодСтроки(Строка) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьКодСтроки(Строка));
```
</details>

## НайтиСтрокуВЗагружаемомОтчете
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НайтиСтрокуВЗагружаемомОтчете(СекцияИзЗагружаемогоОтчета, КодСтроки) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.НайтиСтрокуВЗагружаемомОтчете(СекцияИзЗагружаемогоОтчета, КодСтроки));
```
</details>

## ЗагрузитьПростуюСтроку
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗагрузитьПростуюСтроку(КонтекстФормы, СекцияИзЗагружаемогоОтчета, Строка, КодСтроки, ТабДок) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ЗагрузитьПростуюСтроку(КонтекстФормы, СекцияИзЗагружаемогоОтчета, Строка, КодСтроки, ТабДок));
```
</details>

## ИмяПервойЯчейкиОбластиДляЗаполнения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИмяПервойЯчейкиОбластиДляЗаполнения(ТабДок, ОбластьДокумента) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ИмяПервойЯчейкиОбластиДляЗаполнения(ТабДок, ОбластьДокумента));
```
</details>

## СпециальногоДобавленияСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СпециальногоДобавленияСтроки(КонтекстФормы, ТекТабличноеПоле, НомерВерхЯчейкиОбласти, НомерНижнЯчейкиОбласти) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.СпециальногоДобавленияСтроки(КонтекстФормы, ТекТабличноеПоле, НомерВерхЯчейкиОбласти, НомерНижнЯчейкиОбласти) 
```
</details>

## ДобавитьДополнительнуюСтроку
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьДополнительнуюСтроку(КонтекстФормы, ИмяПервойЯчейкиСтроки, ТабДок = Неопределено) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ДобавитьДополнительнуюСтроку(КонтекстФормы, ИмяПервойЯчейкиСтроки, ТабДок);
```
</details>

## ЗагрузитьМногострочнуюСтроку
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗагрузитьМногострочнуюСтроку(КонтекстФормы, СекцияИзЗагружаемогоОтчета, Строка, КодСтроки, ТабДок) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ЗагрузитьМногострочнуюСтроку(КонтекстФормы, СекцияИзЗагружаемогоОтчета, Строка, КодСтроки, ТабДок));
```
</details>

## ЗагрузитьСекцию
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗагрузитьСекцию(КонтекстФормы, СекцияИзЗагружаемогоОтчета, Секция, Код) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ЗагрузитьСекцию(КонтекстФормы, СекцияИзЗагружаемогоОтчета, Секция, Код));
```
</details>

## РасчетПриЗагрузке
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура РасчетПриЗагрузке(КонтекстФормы, ТабДок, ИмяТекущейСтраницы, ОбластьИмя = "") Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.РасчетПриЗагрузке(КонтекстФормы, ТабДок, ИмяТекущейСтраницы, ОбластьИмя);
```
</details>

## СформироватьСтруктураДанныхОтчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СформироватьСтруктураДанныхОтчета(КонтекстФормы, ДеревоВыгрузки, ПараметрыSections) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.СформироватьСтруктураДанныхОтчета(КонтекстФормы, ДеревоВыгрузки, ПараметрыSections));
```
</details>

## ПоместитьДанныеВОтчет
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПоместитьДанныеВОтчет(КонтекстФормы, ПараметрыSections) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ПоместитьДанныеВОтчет(КонтекстФормы, ПараметрыSections));
```
</details>

## СформироватьТитульный
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СформироватьТитульный(КонтекстФормы, ПараметрыTitle) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.СформироватьТитульный(КонтекстФормы, ПараметрыTitle));
```
</details>

## ЗагрузитьОтчет
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗагрузитьОтчет(КонтекстФормы, ВХ, Ошибка, ПараметрСтрокаXML = Ложь) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ЗагрузитьОтчет(КонтекстФормы, ВХ, Ошибка, ПараметрСтрокаXML);
```
</details>

## ДобавитьСправочникВТаблицу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьСправочникВТаблицу(КонтекстФормы, УзелСправочник) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ДобавитьСправочникВТаблицу(КонтекстФормы, УзелСправочник));
```
</details>

## ЗагрузитьФайлXML
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗагрузитьФайлXML(ИмяФайла) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ЗагрузитьФайлXML(ИмяФайла));
```
</details>

## СформироватьАтрибутыКорневогоЭлементаСхемыВыгрузки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СформироватьАтрибутыКорневогоЭлементаСхемыВыгрузки(КонтекстФормы, ЭлементДереваОтчет) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.СформироватьАтрибутыКорневогоЭлементаСхемыВыгрузки(КонтекстФормы, ЭлементДереваОтчет));
```
</details>

## СформироватьЭлементыРазделаОбщаяИнформацияСхемыВыгрузки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СформироватьЭлементыРазделаОбщаяИнформацияСхемыВыгрузки(КонтекстФормы, ЭлементДереваОбщаяИнформация) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.СформироватьЭлементыРазделаОбщаяИнформацияСхемыВыгрузки(КонтекстФормы, ЭлементДереваОбщаяИнформация));
```
</details>

## ГенерироватьМакетИСтруктуруДанныхРазделаОбщаяИнформация
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ГенерироватьМакетИСтруктуруДанныхРазделаОбщаяИнформация(КонтекстФормы) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ГенерироватьМакетИСтруктуруДанныхРазделаОбщаяИнформация(КонтекстФормы));
```
</details>

## ОписаниеТиповИзСтрокиФормата
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ОписаниеТиповИзСтрокиФормата(СтрокаФорматаЯчейки, МинШиринаЯчейкиВСимв = 0, ТипСтрокойУмолч = "Число") Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ОписаниеТиповИзСтрокиФормата(СтрокаФорматаЯчейки, МинШиринаЯчейкиВСимв, ТипСтрокойУмолч);
```
</details>

## ГенерироватьМакетИСтруктуруДанныхРаздела
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ГенерироватьМакетИСтруктуруДанныхРаздела(КонтекстФормы, УзелСекция, ЭлементДереваРазделыОтчета) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ГенерироватьМакетИСтруктуруДанныхРаздела(КонтекстФормы, УзелСекция, ЭлементДереваРазделыОтчета));
```
</details>

## ДобавитьВРеквизитСписокПараметровКолонок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьВРеквизитСписокПараметровКолонок(КонтекстФормы, КодСекции, СписокПараметровКолонок) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ДобавитьВРеквизитСписокПараметровКолонок(КонтекстФормы, КодСекции, СписокПараметровКолонок));
```
</details>

## СформироватьСтрокуМакетаРаздела
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СформироватьСтрокуМакетаРаздела(КонтекстФормы, МакетРаздела, МакетОбразцовОбластей, СписокПараметровКолонок, ПараметрыСтроки, ЭлементДереваРазделОтчета) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.СформироватьСтрокуМакетаРаздела(КонтекстФормы, МакетРаздела, МакетОбразцовОбластей, СписокПараметровКолонок, ПараметрыСтроки, ЭлементДереваРазделОтчета));
```
</details>

## УстановитьШиринуКолонокМакетаРаздела
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьШиринуКолонокМакетаРаздела(МакетРаздела, СписокПараметровКолонок); Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.УстановитьШиринуКолонокМакетаРаздела(МакетРаздела, СписокПараметровКолонок););
```
</details>

## ДобавитьКОписаниюЯчеекСсылкиНаЗависимыеЯчейки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьКОписаниюЯчеекСсылкиНаЗависимыеЯчейки(КонтекстФормы) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ДобавитьКОписаниюЯчеекСсылкиНаЗависимыеЯчейки(КонтекстФормы));
```
</details>

## ДобавитьКонтрольноеСоотношениеВТаблицу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьКонтрольноеСоотношениеВТаблицу(КонтекстФормы, УзелКонтрольноеСоотношение) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ДобавитьКонтрольноеСоотношениеВТаблицу(КонтекстФормы, УзелКонтрольноеСоотношение));
```
</details>

## ПолучитьАттрибутОбязательностьКонтроля
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьАттрибутОбязательностьКонтроля(УзелКонтрольноеСоотношение) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьАттрибутОбязательностьКонтроля(УзелКонтрольноеСоотношение) 
```
</details>

## СтрокаСНеразрывнымПробелом
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтрокаСНеразрывнымПробелом(ИсхСтрока) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СтрокаСНеразрывнымПробелом(ИсхСтрока));
```
</details>

## ПрименитьПараметрыЯчейки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПрименитьПараметрыЯчейки(КонтекстФормы, ЦелеваяЯчейка, ПараметрыЯчейки, ПараметрыКолонки, ПараметрыСтроки, ЭлементДереваСтрокаРаздела) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ПрименитьПараметрыЯчейки(КонтекстФормы, ЦелеваяЯчейка, ПараметрыЯчейки, ПараметрыКолонки, ПараметрыСтроки, ЭлементДереваСтрокаРаздела));
```
</details>

## РезультирующиеПараметрыЯчейки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РезультирующиеПараметрыЯчейки(ПараметрыКолонки, ПараметрыСтроки) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.РезультирующиеПараметрыЯчейки(ПараметрыКолонки, ПараметрыСтроки));
```
</details>

## РезультатВычисленияУсловияДляЯчейки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РезультатВычисленияУсловияДляЯчейки(КонтекстФормы, Выражение) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.РезультатВычисленияУсловияДляЯчейки(КонтекстФормы, Выражение));
```
</details>

## ПроверитьКонтрольныеСоотношенияВОтчетеВФоне
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПроверитьКонтрольныеСоотношенияВОтчетеВФоне(ПараметрыДляПроверки, АдресХранилища) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ПроверитьКонтрольныеСоотношенияВОтчетеВФоне(ПараметрыДляПроверки, АдресХранилища) 
```
</details>

## ПроверитьКонтрольныеСоотношенияВОтчете
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПроверитьКонтрольныеСоотношенияВОтчете(ПараметрыДляПроверки) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПроверитьКонтрольныеСоотношенияВОтчете(ПараметрыДляПроверки) 
```
</details>

## ВидыОбрабатываемыхНеравенств
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВидыОбрабатываемыхНеравенств() Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ВидыОбрабатываемыхНеравенств());
```
</details>

## КоличествоВхожденийНеравенств
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция КоличествоВхожденийНеравенств(Формула, ВидыНеравенств) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.КоличествоВхожденийНеравенств(Формула, ВидыНеравенств));
```
</details>

## РазложитьФормулуНаДвеПодформулы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура РазложитьФормулуНаДвеПодформулы(Формула, Подформула1, Подформула2, ВидыНеравенств) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.РазложитьФормулуНаДвеПодформулы(Формула, Подформула1, Подформула2, ВидыНеравенств));
```
</details>

## ФормулаСодержитЛогическиеВыражения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ФормулаСодержитЛогическиеВыражения(Формула) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ФормулаСодержитЛогическиеВыражения(Формула));
```
</details>

## РазложитьФормулуНаПодформулы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РазложитьФормулуНаПодформулы(Формула, ВидыНеравенств) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.РазложитьФормулуНаПодформулы(Формула, ВидыНеравенств));
```
</details>

## РезультатПроверкиКонтрольногоСоотношения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РезультатПроверкиКонтрольногоСоотношения(ПараметрыДляПроверки, Формула, Условие, УсловиеПериод, РазрядностьОкругления, ДопустимоеОтклонение) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.РезультатПроверкиКонтрольногоСоотношения(ПараметрыДляПроверки, Формула, Условие, УсловиеПериод, РазрядностьОкругления, ДопустимоеОтклонение) 
```
</details>

## ПолучитьМатрицу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьМатрицу(КоличествоСтрок,КоличествоСтолбцов) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьМатрицу(КоличествоСтрок, КоличествоСтолбцов));
```
</details>

## ПолучитьСтроковыйКлюч
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСтроковыйКлюч(Парам) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьСтроковыйКлюч(Парам));
```
</details>

## СтроковыйКлючВПараметры
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтроковыйКлючВПараметры(Ключ) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СтроковыйКлючВПараметры(Ключ));
```
</details>

## ДополнитьЗначенияЭлементов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДополнитьЗначенияЭлементов(ТаблицаЭлементов) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ДополнитьЗначенияЭлементов(ТаблицаЭлементов));
```
</details>

## ВекторныйРезультатВычисленияВыраженияИзСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВекторныйРезультатВычисленияВыраженияИзСтроки(ПараметрыДляПроверки, ТекстовоеПредставлениеВыражения, РазрядностьОкругления = 2, Допустимоетклонение = 0, МатрицаУсловий = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ВекторныйРезультатВычисленияВыраженияИзСтроки(ПараметрыДляПроверки, ТекстовоеПредставлениеВыражения, РазрядностьОкругления, Допустимоетклонение, МатрицаУсловий);
```
</details>

## ВекторныйРезультатВычисленияЛогическогоПодвыражения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВекторныйРезультатВычисленияЛогическогоПодвыражения(ПараметрыДляПроверки, ЭтоМежПериоднаяПроверка, МатрицаРезультата, ШаблонВыражения, ТаблицаЭлементов, ИндексПодвыражения = 0, СчетчикОшибок = 0) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ВекторныйРезультатВычисленияЛогическогоПодвыражения(ПараметрыДляПроверки, ЭтоМежПериоднаяПроверка, МатрицаРезультата, ШаблонВыражения, ТаблицаЭлементов, ИндексПодвыражения, СчетчикОшибок);
```
</details>

## РезультатВычисленияВыраженияИзСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РезультатВычисленияВыраженияИзСтроки(ПараметрыДляПроверки, ТекстовоеПредставлениеВыражения, РазрядностьОкругления = 2, ДопустимоеОтклонение = 0) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.РезультатВычисленияВыраженияИзСтроки(ПараметрыДляПроверки, ТекстовоеПредставлениеВыражения, РазрядностьОкругления, ДопустимоеОтклонение);
```
</details>

## ВыделитьЭлементыИзСтрокиВыражения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВыделитьЭлементыИзСтрокиВыражения(ПараметрыДляПроверки, ТекстовоеВыражение, ТаблицаЭлементов); Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ВыделитьЭлементыИзСтрокиВыражения(ПараметрыДляПроверки, ТекстовоеВыражение, ТаблицаЭлементов););
```
</details>

## ОпределитьОбластьДействияОператораСуммы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОпределитьОбластьДействияОператораСуммы(ТекстовоеВыражение, ТаблицаЭлементов, СчетчикОшибок = 0) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ОпределитьОбластьДействияОператораСуммы(ТекстовоеВыражение, ТаблицаЭлементов, СчетчикОшибок);
```
</details>

## ПолучитьЗначенияОбластейЭлементов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьЗначенияОбластейЭлементов(ПараметрыДляПроверки, ТаблицаЭлементов, РазрядностьОкругления, СчетчикОшибок = 0) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ПолучитьЗначенияОбластейЭлементов(ПараметрыДляПроверки, ТаблицаЭлементов, РазрядностьОкругления, СчетчикОшибок);
```
</details>

## ВыполнитьЗаменуФункцийЯзыкаКС
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВыполнитьЗаменуФункцийЯзыкаКС(ТекстовоеВыражение) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ВыполнитьЗаменуФункцийЯзыкаКС(ТекстовоеВыражение) 
```
</details>

## ВычислитьИЗаменитьУсловиеНаПериод
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВычислитьИЗаменитьУсловиеНаПериод(ПараметрыДляПроверки, ТекстовоеВыражение) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ВычислитьИЗаменитьУсловиеНаПериод(ПараметрыДляПроверки, ТекстовоеВыражение));
```
</details>

## МассивЛогическихПодвыраженийВыделитьИНазначитьИндексВТаблице
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МассивЛогическихПодвыраженийВыделитьИНазначитьИндексВТаблице(ТекстовоеВыражение, ТаблицаЭлементов) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.МассивЛогическихПодвыраженийВыделитьИНазначитьИндексВТаблице(ТекстовоеВыражение, ТаблицаЭлементов) 
```
</details>

## РезультатВычисленияЛогическогоПодвыражения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РезультатВычисленияЛогическогоПодвыражения(ПараметрыДляПроверки, ШаблонВыражения, ТаблицаЭлементов, ИндексПодвыражения = 0, СчетчикОшибок = 0) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.РезультатВычисленияЛогическогоПодвыражения(ПараметрыДляПроверки, ШаблонВыражения, ТаблицаЭлементов, ИндексПодвыражения, СчетчикОшибок);
```
</details>

## СтруктураСоставляющихЭлемента
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураСоставляющихЭлемента(Элемент) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СтруктураСоставляющихЭлемента(Элемент) 
```
</details>

## МассивЗначенийИзСтрСпискаИДиапазонов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МассивЗначенийИзСтрСпискаИДиапазонов(Стр, СтрРазделитель = ",", ИскатьДиапазон = Истина, СчетчикОшибок = 0) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.МассивЗначенийИзСтрСпискаИДиапазонов(Стр, СтрРазделитель, ", ИскатьДиапазон, СчетчикОшибок);
```
</details>

## ИдентификаторЭлементаСпискаФормПоПредставлению
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИдентификаторЭлементаСпискаФормПоПредставлению(ПараметрыДляПроверки, ПредставлениеЭлемента); Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ИдентификаторЭлементаСпискаФормПоПредставлению(ПараметрыДляПроверки, ПредставлениеЭлемента););
```
</details>

## РезультатУсловияНаПериод
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РезультатУсловияНаПериод(ПараметрыДляПроверки, ЛогОператор, КодыПериодов) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.РезультатУсловияНаПериод(ПараметрыДляПроверки, ЛогОператор, КодыПериодов));
```
</details>

## ЧислоИзСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЧислоИзСтроки(Стр, СчетчикОшибок = 0) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ЧислоИзСтроки(Стр, СчетчикОшибок);
```
</details>

## ПодвыражениеИзСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПодвыражениеИзСтроки(Стр, СтрОстаток = "", ВыделитьСлева = Истина) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПодвыражениеИзСтроки(Стр, СтрОстаток, ВыделитьСлева);
```
</details>

## ИмяПоказателяБезИндексаИСоставляющиеАдреса
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИмяПоказателяБезИндексаИСоставляющиеАдреса(ИмяПоказателя, ИндексПоказателя = 0, Многострочный = Ложь, ПолучитьСоставляющие = Ложь, НомерРаздела = 0, НомерСтроки = 0, КодКолонки = ""); Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ИмяПоказателяБезИндексаИСоставляющиеАдреса(ИмяПоказателя, ИндексПоказателя, Многострочный, ПолучитьСоставляющие, НомерРаздела, НомерСтроки, КодКолонки);
```
</details>

## ИмяПоказателяЯчейки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИмяПоказателяЯчейки(КодРаздела, КодСтроки, КодКолонки, Многострочный = Ложь) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ИмяПоказателяЯчейки(КодРаздела, КодСтроки, КодКолонки, Многострочный);
```
</details>

## РезультатПроверкиЗначенияСпецифики
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РезультатПроверкиЗначенияСпецифики(Значение, МассивЗначенийСпецифик) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.РезультатПроверкиЗначенияСпецифики(Значение, МассивЗначенийСпецифик) 
```
</details>

## ЗначениеПоказателяИзТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗначениеПоказателяИзТаблицы(ЭлементВыражения, ИндСтроки, ИндГрафы) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ЗначениеПоказателяИзТаблицы(ЭлементВыражения, ИндСтроки, ИндГрафы) 
```
</details>

## СоответствиеФункцийРеализациям
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СоответствиеФункцийРеализациям() Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СоответствиеФункцийРеализациям() 
```
</details>

## РезультатВычисленияЭлементарногоВыражения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РезультатВычисленияЭлементарногоВыражения(ВычисляемоеВыражение, СчетчикОшибок) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.РезультатВычисленияЭлементарногоВыражения(ВычисляемоеВыражение, СчетчикОшибок) 
```
</details>

## НомерПериодаИзСправочника
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НомерПериодаИзСправочника(ПараметрыДляПроверки, СтрКодПериода) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.НомерПериодаИзСправочника(ПараметрыДляПроверки, СтрКодПериода));
```
</details>

## АбсЗначение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция АбсЗначение(Выражение) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.АбсЗначение(Выражение) 
```
</details>

## ПервоеЗаполненное
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПервоеЗаполненное(В1 = Неопределено, В2 = Неопределено, В3 = Неопределено, В4 = Неопределено, В5 = Неопределено, Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПервоеЗаполненное(В1, В2, В3, В4, В5, );
```
</details>

## НаибольшееЦелое
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НаибольшееЦелое(Выражение) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.НаибольшееЦелое(Выражение) 
```
</details>

## ЕслиПусто
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЕслиПусто(Выражение, ЗамещающееЗначение) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ЕслиПусто(Выражение, ЗамещающееЗначение) 
```
</details>

## НеопределеноЕсли
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НеопределеноЕсли(Выражение1, Выражение2) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.НеопределеноЕсли(Выражение1, Выражение2) 
```
</details>

## ОкрЗначение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ОкрЗначение(Выражение, Точность, ТипОперации = 0) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ОкрЗначение(Выражение, Точность, ТипОперации);
```
</details>

## ПреобразоватьВыражение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПреобразоватьВыражение(Выражение) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ПреобразоватьВыражение(Выражение) 
```
</details>

## ПозНайденногоИзМассива
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПозНайденногоИзМассива(Стр, МассивПодстрок, ДлинаНайденнойПодстроки = 0) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПозНайденногоИзМассива(Стр, МассивПодстрок, ДлинаНайденнойПодстроки);
```
</details>

## ПорядковыйНомерПериодаВГоду
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПорядковыйНомерПериодаВГоду(ДатаВПериоде, Периодичность) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПорядковыйНомерПериодаВГоду(ДатаВПериоде, Периодичность));
```
</details>

## ПолучитьШаблон
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьШаблон(ИмяФормы, ИмяМакетаШаблона) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьШаблон(ИмяФормы, ИмяМакетаШаблона) 
```
</details>

## ПолучитьПараметрыИнициализацииУниверсальногоОтчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПараметрыИнициализацииУниверсальногоОтчета(Форма, ИмяМакетаАтрибутов, ИмяМакетаШаблона, ИмяСхемы, ФормироватьТекстВыгрузки = Истина, ЗагружатьШаблон = Истина) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьПараметрыИнициализацииУниверсальногоОтчета(Форма, ИмяМакетаАтрибутов, ИмяМакетаШаблона, ИмяСхемы, ФормироватьТекстВыгрузки, ЗагружатьШаблон);
```
</details>

## ПолучитьРеквизитФормыОбособленноеПодразделение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьРеквизитФормыОбособленноеПодразделение(Форма) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьРеквизитФормыОбособленноеПодразделение(Форма) 
```
</details>

## ПолучитьСведенияОШаблоне
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСведенияОШаблоне(Форма, ИмяМакетаАтрибутов) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьСведенияОШаблоне(Форма, ИмяМакетаАтрибутов) 
```
</details>

## ТекстВыгрузкиОтчетаСтатистики
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ТекстВыгрузкиОтчетаСтатистики(мСохраненныйДок, ПараметрыДляПроверки) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ТекстВыгрузкиОтчетаСтатистики(мСохраненныйДок, ПараметрыДляПроверки));
```
</details>

## СформироватьКонтекстФормыДляПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СформироватьКонтекстФормыДляПоказателей(мСохраненныйДок) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СформироватьКонтекстФормыДляПоказателей(мСохраненныйДок) 
```
</details>

## ПолучитьДокументПериода
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьДокументПериода(ПредидущийПериод, ОбособленноеПодразделение) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьДокументПериода(ПредидущийПериод, ОбособленноеПодразделение));
```
</details>

## ЗаполнитьДаннымиУзел
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнитьДаннымиУзел(Параметры, Узел, НомерСтроки = Неопределено) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ЗаполнитьДаннымиУзел(Параметры, Узел, НомерСтроки);
```
</details>

## ЗаполнитьДанными
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнитьДанными(КонтекстФормы, ДеревоВыгрузки, ПараметрыВыгрузки) Экспорт
```

Пример вызова
```bsl
УниверсальныйОтчетСтатистики.ЗаполнитьДанными(КонтекстФормы, ДеревоВыгрузки, ПараметрыВыгрузки) 
```
</details>

## СформироватьСтруктуруПараметров
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СформироватьСтруктуруПараметров(КонтекстФормы, мСохраненныйДок, ИмяМакетаАттрибутов) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СформироватьСтруктуруПараметров(КонтекстФормы, мСохраненныйДок, ИмяМакетаАттрибутов) 
```
</details>

## ПолучитьПостроительПоПараметру
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПостроительПоПараметру(Шаблон) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьПостроительПоПараметру(Шаблон));
```
</details>

## ДокументСоответствуетПравиламРосстата
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ДокументСоответствуетПравиламРосстата(Документ) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ДокументСоответствуетПравиламРосстата(Документ));
```
</details>

## ПолучитьУзелЗаголовок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьУзелЗаголовок(Документ) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьУзелЗаголовок(Документ));
```
</details>

## ЗаголовкиДокументовСовпадают
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗаголовкиДокументовСовпадают(Документ1, Документ2) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ЗаголовкиДокументовСовпадают(Документ1, Документ2));
```
</details>

## ПолучитьСекции
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСекции(Документ) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьСекции(Документ));
```
</details>

## ПолучитьСтрокиУзла
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСтрокиУзла(Узел) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьСтрокиУзла(Узел));
```
</details>

## ПолучитьКолонкиУзла
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьКолонкиУзла(Узел) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьКолонкиУзла(Узел));
```
</details>

## СформироватьСвойстваКолонок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СформироватьСвойстваКолонок(Колонки) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СформироватьСвойстваКолонок(Колонки));
```
</details>

## СравнитьКолонки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СравнитьКолонки(Колонки1, Колонки2, Свойства1, Свойства2, Поля) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СравнитьКолонки(Колонки1, Колонки2, Свойства1, Свойства2, Поля));
```
</details>

## СформироватьСвойстваСтрок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СформироватьСвойстваСтрок(Строки, СвойстваКолонок) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СформироватьСвойстваСтрок(Строки, СвойстваКолонок));
```
</details>

## СравнитьСтроку
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СравнитьСтроку(Стр1, Стр2, Поля) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СравнитьСтроку(Стр1, Стр2, Поля));
```
</details>

## СравнитьСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СравнитьСтроки(Строки1, Строки2, СвойстваКолонок1, СвойстваКолонок2, Поля) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СравнитьСтроки(Строки1, Строки2, СвойстваКолонок1, СвойстваКолонок2, Поля));
```
</details>

## МетаданныеСекцииСовпадают
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МетаданныеСекцииСовпадают(Секция1, Секция2) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.МетаданныеСекцииСовпадают(Секция1, Секция2));
```
</details>

## МетаданныеСодержательнойЧастиДокументовСовпадают
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МетаданныеСодержательнойЧастиДокументовСовпадают(Документ1, Документ2) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.МетаданныеСодержательнойЧастиДокументовСовпадают(Документ1, Документ2));
```
</details>

## ПолучитьСправочникПериодов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСправочникПериодов(Документ) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.ПолучитьСправочникПериодов(Документ));
```
</details>

## СправочникиПериодовСовпадают
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СправочникиПериодовСовпадают(Документ1, Документ2) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СправочникиПериодовСовпадают(Документ1, Документ2));
```
</details>

## СравнитьСтруктурыШаблонов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СравнитьСтруктурыШаблонов(Шаблон1, Шаблон2) Экспорт
```

Пример вызова
```bsl
Результат = УниверсальныйОтчетСтатистики.СравнитьСтруктурыШаблонов(Шаблон1, Шаблон2) 
```
</details>

---

© 2024, **ООО 1С-Софт**  
Все права защищены. Эта программа и сопроводительные материалы предоставляются  
в соответствии с условиями лицензии [Attribution 4.0 International (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/legalcode).

---
