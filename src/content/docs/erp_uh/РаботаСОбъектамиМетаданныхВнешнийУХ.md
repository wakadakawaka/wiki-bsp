---
title: Работа с объектами метаданных внешний УХ
slug: erp-uh/работа-с-объектами-метаданных-внешний-ух
---


## ПолучитьXMLTypeOf
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьXMLTypeOf(База, Значение) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьXMLTypeOf(База, Значение) 
```
</details>

## ПолучитьXMLType
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьXMLType(База, Тип) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьXMLType(База, Тип) 
```
</details>

## СравнитьЗначения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СравнитьЗначения(База,Значение1,Значение2) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СравнитьЗначения(База, Значение1, Значение2) 
```
</details>

## ПолучитьFromXMLType
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьFromXMLType(База,Тип) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьFromXMLType(База, Тип) 
```
</details>

## ПолучитьXMLString
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьXMLString(База,Значение) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьXMLString(База, Значение) 
```
</details>

## ПолучитьXMLValue
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьXMLValue(База,Тип,СтрокаXML) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьXMLValue(База, Тип, СтрокаXML) 
```
</details>

## ПолучитьЗначениеЗаполнено
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьЗначениеЗаполнено(База,Значение) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьЗначениеЗаполнено(База, Значение) 
```
</details>

## ПолучитьЗаполнитьЗначенияСвойств
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьЗаполнитьЗначенияСвойств(База,Приемник,Источник) Экспорт
```

Пример вызова
```bsl
РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьЗаполнитьЗначенияСвойств(База, Приемник, Источник) 
```
</details>

## ПолучитьString
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьString(База,Значение) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьString(База, Значение) 
```
</details>

## МетаданныеНайтиПоТипу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МетаданныеНайтиПоТипу(База, Тип, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.МетаданныеНайтиПоТипу(База, Тип, Кэш);
```
</details>

## МетаданныеНайтиПоПолномуИмени
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МетаданныеНайтиПоПолномуИмени(База,ПолноеИмя) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.МетаданныеНайтиПоПолномуИмени(База, ПолноеИмя) 
```
</details>

## ПолучитьОбъектИнтеграции
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОбъектИнтеграции(ИмяОбъекта,ТекСоединениеВИБ=Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьОбъектИнтеграции(ИмяОбъекта, ТекСоединениеВИБ);
```
</details>

## ПримитивныйТип
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПримитивныйТип(Тип) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПримитивныйТип(Тип) 
```
</details>

## ВернутьИндексВМассиве
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВернутьИндексВМассиве(МассивДанные, Элемент) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВернутьИндексВМассиве(МассивДанные, Элемент) 
```
</details>

## ПримитивныйТипСтрока
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Служебная функция, определяющая примитивность переданного типа
//
Функция ПримитивныйТипСтрока(СтрокаТип) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПримитивныйТипСтрока(СтрокаТип) 
```
</details>

## ПолучитьНаименованиеСсылочногоТипаБД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьНаименованиеСсылочногоТипаБД(База, Тип) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьНаименованиеСсылочногоТипаБД(База, Тип) 
```
</details>

## ПолучитьМассивИменОбъектов8x
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьМассивИменОбъектов8x(ТипОбъектаМетаданных,ТекСоединениеВИБ) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьМассивИменОбъектов8x(ТипОбъектаМетаданных, ТекСоединениеВИБ) 
```
</details>

## ПолучитьМассивСтруктурПлановСчетовБД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьМассивСтруктурПлановСчетовБД(ТекСоединениеВИБ) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьМассивСтруктурПлановСчетовБД(ТекСоединениеВИБ));
```
</details>

## ОписаниеОбщихРеквизитов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ОписаниеОбщихРеквизитов(ТекСоединениеВИБ, МетаданныеИБ, Кэш) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ОписаниеОбщихРеквизитов(ТекСоединениеВИБ, МетаданныеИБ, Кэш));
```
</details>

## ДобавитьОписаниеОбщихРеквизитовСправочника
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ДобавитьОписаниеОбщихРеквизитовСправочника(ШаблонОписания, СправочникБД, МетаданныеИБ, База, Кэш) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ДобавитьОписаниеОбщихРеквизитовСправочника(ШаблонОписания, СправочникБД, МетаданныеИБ, База, Кэш));
```
</details>

## СтруктураОписанияСправочника8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураОписанияСправочника8х(ШаблонОписания, ТекСоединениеВИБ, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СтруктураОписанияСправочника8х(ШаблонОписания, ТекСоединениеВИБ, Кэш);
```
</details>

## ПолучитьПредопределенныеНаборыСвойств
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьПредопределенныеНаборыСвойств(ТипОбъектаМетаданных,ШаблонОписания,МетаданныеИБ,ТекСоединениеВИБ,Кэш=Неопределено) Экспорт
```

Пример вызова
```bsl
РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьПредопределенныеНаборыСвойств(ТипОбъектаМетаданных, ШаблонОписания, МетаданныеИБ, ТекСоединениеВИБ, Кэш);
```
</details>

## ОписаниеТипаДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ОписаниеТипаДанных(ТекСоединениеВИБ, ОписаниеТипа, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ОписаниеТипаДанных(ТекСоединениеВИБ, ОписаниеТипа, Кэш);
```
</details>

## ПолучитьМассивСтруктурТабличныхЧастей1CПредприятие8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьМассивСтруктурТабличныхЧастей1CПредприятие8х(ДанныеОбъектаВладельцаТЧ, ТекСоединениеВИБ, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьМассивСтруктурТабличныхЧастей1CПредприятие8х(ДанныеОбъектаВладельцаТЧ, ТекСоединениеВИБ, Кэш);
```
</details>

## СтруктураОписанияДокумента8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураОписанияДокумента8х(ШаблонОписания, ТекСоединениеВИБ, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СтруктураОписанияДокумента8х(ШаблонОписания, ТекСоединениеВИБ, Кэш);
```
</details>

## СтруктураОписанияПланаВидовХарактеристик8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураОписанияПланаВидовХарактеристик8х(ШаблонОписания, ТекСоединениеВИБ, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СтруктураОписанияПланаВидовХарактеристик8х(ШаблонОписания, ТекСоединениеВИБ, Кэш);
```
</details>

## СтруктураОписанияПеречисления8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураОписанияПеречисления8х(ШаблонОписания, ТекСоединениеВИБ) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СтруктураОписанияПеречисления8х(ШаблонОписания, ТекСоединениеВИБ) 
```
</details>

## СтруктураОписанияРегистраНакопления8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураОписанияРегистраНакопления8х(ШаблонОписания, ТекСоединениеВИБ, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СтруктураОписанияРегистраНакопления8х(ШаблонОписания, ТекСоединениеВИБ, Кэш);
```
</details>

## СтруктураОписанияРегистраБухгалтерии8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураОписанияРегистраБухгалтерии8х(ШаблонОписания, ТекСоединениеВИБ, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СтруктураОписанияРегистраБухгалтерии8х(ШаблонОписания, ТекСоединениеВИБ, Кэш);
```
</details>

## СтруктураОписанияПланаСчетов8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураОписанияПланаСчетов8х(ШаблонОписания, ТекСоединениеВИБ, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СтруктураОписанияПланаСчетов8х(ШаблонОписания, ТекСоединениеВИБ, Кэш);
```
</details>

## СтруктураОписанияРегистраСведений8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураОписанияРегистраСведений8х(ШаблонОписания, ТекСоединениеВИБ, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СтруктураОписанияРегистраСведений8х(ШаблонОписания, ТекСоединениеВИБ, Кэш);
```
</details>

## ПолучитьОписаниеПримитивногоТипаИзБД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОписаниеПримитивногоТипаИзБД(База, ОписаниеТиповБД, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьОписаниеПримитивногоТипаИзБД(База, ОписаниеТиповБД, Кэш);
```
</details>

## ПолучитьСтрокуПримитивныхТипов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСтрокуПримитивныхТипов(ОписаниеТипов, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьСтрокуПримитивныхТипов(ОписаниеТипов, Кэш);
```
</details>

## ПолучитьСтрокуТиповДанныхБД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСтрокуТиповДанныхБД(ТекСоединениеВИБ, ОписаниеТиповБД, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьСтрокуТиповДанныхБД(ТекСоединениеВИБ, ОписаниеТиповБД, Кэш);
```
</details>

## СтрокаПримитивногоТипа
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтрокаПримитивногоТипа(XMLType, ПримитивныеXMLТипы) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СтрокаПримитивногоТипа(XMLType, ПримитивныеXMLТипы));
```
</details>

## ПреобразоватьТИПизВИБ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПреобразоватьТИПизВИБ(База, Тип, ВозвращатьТип = Истина, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПреобразоватьТИПизВИБ(База, Тип, ВозвращатьТип, Кэш);
```
</details>

## ПолучитьРабочееПредставлениеТипа
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьРабочееПредставлениеТипа(ПолноеИмяТипа) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьРабочееПредставлениеТипа(ПолноеИмяТипа) 
```
</details>

## ПолучитьОписаниеТиповСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОписаниеТиповСтроки(ДлинаСтроки) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьОписаниеТиповСтроки(ДлинаСтроки) 
```
</details>

## ВИБ_ПолучитьПустуюСсылкуПоТаблицеАналитики
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ПолучитьПустуюСсылкуПоТаблицеАналитики(ТекСоединениеВИБ,ТаблицаАналитикиВИБ) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьПустуюСсылкуПоТаблицеАналитики(ТекСоединениеВИБ, ТаблицаАналитикиВИБ) 
```
</details>

## ПолучитьСсылкуПОСтрокеUUID
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСсылкуПОСтрокеUUID(База,ТипОбъектаМетаданных,ИмяОбъектаМетаданных,СтрокаUUID) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьСсылкуПОСтрокеUUID(База, ТипОбъектаМетаданных, ИмяОбъектаМетаданных, СтрокаUUID) 
```
</details>

## ПолучитьСсылкуИзТаблицыПОСтрокеUUID
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСсылкуИзТаблицыПОСтрокеUUID(База, ТаблицаАналитикиВИБ,СтрокаUUID) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьСсылкуИзТаблицыПОСтрокеUUID(База, ТаблицаАналитикиВИБ, СтрокаUUID) 
```
</details>

## РазложитьСтрокуВМассивПодстрок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Разбивает строку на несколько строк по указанному разделителю. Разделитель может иметь любую длину.
// В случаях, когда разделителем является строка из одного символа, и не используется параметр СокращатьНепечатаемыеСимволы,
// рекомендуется использовать функцию платформы СтрРазделить.
//
// Параметры:
//  Значение               - Строка - текст с разделителями;
//  Разделитель            - Строка - разделитель строк текста, минимум 1 символ;
//  ПропускатьПустыеСтроки - Булево - признак необходимости включения в результат пустых строк.
//    Если параметр не задан, то функция работает в режиме совместимости со своей предыдущей версией:
//     - для разделителя-пробела пустые строки не включаются в результат, для остальных разделителей пустые строки
//       включаются в результат.
//     - если параметр Строка не содержит значащих символов или не содержит ни одного символа (пустая строка), то в
//       случае разделителя-пробела результатом функции будет массив, содержащий одно значение "" (пустая строка), а
//       при других разделителях результатом функции будет пустой массив.
//  СокращатьНепечатаемыеСимволы - Булево - сокращать непечатаемые символы по краям каждой из найденных подстрок.
//
// Возвращаемое значение:
//  Массив - массив строк.
//
Функция РазложитьСтрокуВМассивПодстрок(Знач Значение, Знач Разделитель = ",", Знач ПропускатьПустыеСтроки = Неопределено, Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.РазложитьСтрокуВМассивПодстрок(Значение, Разделитель, ", ПропускатьПустыеСтроки, );
```
</details>

## СтруктураОписанияПроизвольногоЗапроса8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураОписанияПроизвольногоЗапроса8х(ТекСоединениеВИБ, ТекстЗапросаМодуля, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СтруктураОписанияПроизвольногоЗапроса8х(ТекСоединениеВИБ, ТекстЗапросаМодуля, Кэш);
```
</details>

## ПолучитьТаблицуИдентификаторовОбъектов8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТаблицуИдентификаторовОбъектов8х(ТекСоединениеВИБ,ТаблицаАналитики) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьТаблицуИдентификаторовОбъектов8х(ТекСоединениеВИБ, ТаблицаАналитики) 
```
</details>

## ПодготовитьПроизвольныйЗапросДляПолученияДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПодготовитьПроизвольныйЗапросДляПолученияДанных(База, ДанныеДляЗапроса, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПодготовитьПроизвольныйЗапросДляПолученияДанных(База, ДанныеДляЗапроса, Кэш);
```
</details>

## УстановитьПараметрыЗапроса
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьПараметрыЗапроса(База, ОбъектЗапрос, ИспользуемыеПараметры, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
РаботаСОбъектамиМетаданныхВнешнийУХ.УстановитьПараметрыЗапроса(База, ОбъектЗапрос, ИспользуемыеПараметры, Кэш);
```
</details>

## ПодготовитьПостроительЗапросаДляПолученияДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПодготовитьПостроительЗапросаДляПолученияДанных(База, ДанныеДляЗапроса, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПодготовитьПостроительЗапросаДляПолученияДанных(База, ДанныеДляЗапроса, Кэш);
```
</details>

## ДобавитьОтборУстановитьПараметрСписокЗначений
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьОтборУстановитьПараметрСписокЗначений(База,ОбъектЗапрос,МассивПараметров,СтрокаПараметр,СписокЗначений) Экспорт
```

Пример вызова
```bsl
РаботаСОбъектамиМетаданныхВнешнийУХ.ДобавитьОтборУстановитьПараметрСписокЗначений(База, ОбъектЗапрос, МассивПараметров, СтрокаПараметр, СписокЗначений));
```
</details>

## ДобавитьОтборУстановитьПараметрЗначение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьОтборУстановитьПараметрЗначение(База,ОбъектЗапрос,МассивПараметров,СтрокаПараметр,РабЗначениеПараметра) Экспорт
```

Пример вызова
```bsl
РаботаСОбъектамиМетаданныхВнешнийУХ.ДобавитьОтборУстановитьПараметрЗначение(База, ОбъектЗапрос, МассивПараметров, СтрокаПараметр, РабЗначениеПараметра));
```
</details>

## ПолучитьСписокЗначенийВИБ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСписокЗначенийВИБ(ТекСоединениеВИБ,СписокЗначений) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьСписокЗначенийВИБ(ТекСоединениеВИБ, СписокЗначений) 
```
</details>

## ПолучитьЗначениеПараметраВИБ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьЗначениеПараметраВИБ(ТекСоединениеВИБ,ДанныеПараметра) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьЗначениеПараметраВИБ(ТекСоединениеВИБ, ДанныеПараметра));
```
</details>

## ПолучитьСписокОбъектовВИБ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьСписокОбъектовВИБ(ТекСоединениеВИБ,ДанныеПараметра,СписокЗначенийВИБ) Экспорт
```

Пример вызова
```bsl
РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьСписокОбъектовВИБ(ТекСоединениеВИБ, ДанныеПараметра, СписокЗначенийВИБ));
```
</details>

## ПодготовитьЗапросДляПолученияДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПодготовитьЗапросДляПолученияДанных(База, ДанныеДляЗапроса, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПодготовитьЗапросДляПолученияДанных(База, ДанныеДляЗапроса, Кэш);
```
</details>

## ПолучитьРабочуюТаблицуДанных8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьРабочуюТаблицуДанных8х(ТекСоединениеВИБ, ДанныеДляЗапроса, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьРабочуюТаблицуДанных8х(ТекСоединениеВИБ, ДанныеДляЗапроса, Кэш);
```
</details>

## ПолучитьОписаниеТиповPrim
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОписаниеТиповPrim(База,КолонкаCOM,ТипКОМБулево,ТипКОМЧисло,ТипКОМСтрока,ТипКОМДата) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьОписаниеТиповPrim(База, КолонкаCOM, ТипКОМБулево, ТипКОМЧисло, ТипКОМСтрока, ТипКОМДата));
```
</details>

## ПреобразоватьCOMТаблицуВТаблицуЗначений
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПреобразоватьCOMТаблицуВТаблицуЗначений(База, ТаблицаCOM, ЛокальнаяТаблица, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
РаботаСОбъектамиМетаданныхВнешнийУХ.ПреобразоватьCOMТаблицуВТаблицуЗначений(База, ТаблицаCOM, ЛокальнаяТаблица, Кэш);
```
</details>

## ЗаполнитьДеревоЗначенийДляСправочника8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗаполнитьДеревоЗначенийДляСправочника8х(База,Параметры) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ЗаполнитьДеревоЗначенийДляСправочника8х(База, Параметры) 
```
</details>

## ЗаполнитьВетвьДерева
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Вспомогательная процедура, обеспечивающая рекурсивное чтение данных из выборки значений
//
Процедура ЗаполнитьВетвьДерева(База, СтрокаДерева, Выборка,ПолучатьСсылку) Экспорт
```

Пример вызова
```bsl
РаботаСОбъектамиМетаданныхВнешнийУХ.ЗаполнитьВетвьДерева(База, СтрокаДерева, Выборка, ПолучатьСсылку));
```
</details>

## ЗаполнитьДеревоЗначенийДляПеречисления8х
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗаполнитьДеревоЗначенийДляПеречисления8х(База,Параметры) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ЗаполнитьДеревоЗначенийДляПеречисления8х(База, Параметры) 
```
</details>

## ПолучитьРабочийТипОбъектаМетаданных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьРабочийТипОбъектаМетаданных(ТипОбъектаМетаДанных) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьРабочийТипОбъектаМетаданных(ТипОбъектаМетаДанных) 
```
</details>

## ЗаполнитьДеревоЗначенийИзСОМТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Функция формирует дерево значений на основании полученного результата запроса
// из внешней информационной базы.
Процедура ЗаполнитьДеревоЗначенийИзСОМТаблицы(База, ДеревоЗначений,РезультатЗапроса,ПолучатьСсылку=Ложь) Экспорт
```

Пример вызова
```bsl
РаботаСОбъектамиМетаданныхВнешнийУХ.ЗаполнитьДеревоЗначенийИзСОМТаблицы(База, ДеревоЗначений, РезультатЗапроса, ПолучатьСсылку);
```
</details>

## ПолучитьОписаниеТиповБулево
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОписаниеТиповБулево() Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ПолучитьОписаниеТиповБулево());
```
</details>

## ВИБ_ДобавитьСтрокиПодчиненныхРеквизитов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВИБ_ДобавитьСтрокиПодчиненныхРеквизитов(ТекСоединениеВИБ, Экспорт
```

Пример вызова
```bsl
РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ДобавитьСтрокиПодчиненныхРеквизитов(ТекСоединениеВИБ, );
```
</details>

## ВИБ_ЗаполнитьАналитикуПоПравилам
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ЗаполнитьАналитикуПоПравилам(ТекСоединениеВИБ, ТаблицаДанных, ПравилаИспользованияПолей, СтруктураКлючевыхРеквизитов, Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ЗаполнитьАналитикуПоПравилам(ТекСоединениеВИБ, ТаблицаДанных, ПравилаИспользованияПолей, СтруктураКлючевыхРеквизитов, );
```
</details>

## ВИБ_ПолучитьОписаниеТиповСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Служебная функция, предназначенная для получения описания типов строки, заданной длины.
//
// Параметры:
//  ДлинаСтроки - число, длина строки.
//
// Возвращаемое значение:
//  Объект "ОписаниеТипов" для строки указанной длины.
//
Функция ВИБ_ПолучитьОписаниеТиповСтроки(База,ДлинаСтроки) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьОписаниеТиповСтроки(База, ДлинаСтроки) 
```
</details>

## ВИБ_ПолучитьСоответствиеТаблицДублей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ПолучитьСоответствиеТаблицДублей(ТекСоединениеВИБ, ТабРезультат, ТабГруппировкиДубли, Кэш = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьСоответствиеТаблицДублей(ТекСоединениеВИБ, ТабРезультат, ТабГруппировкиДубли, Кэш);
```
</details>

## ВИБ_ПолучитьОбъектМетаданныхПоТаблицеАналитики
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ПолучитьОбъектМетаданныхПоТаблицеАналитики(ТекСоединениеВИБ,ТаблицаАналитики) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьОбъектМетаданныхПоТаблицеАналитики(ТекСоединениеВИБ, ТаблицаАналитики));
```
</details>

## ВИБ_ПолучитьРеквизитОбъектаМД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ПолучитьРеквизитОбъектаМД(ТекСоединениеВИБ,ИмяРеквизита, ОбъектМД) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьРеквизитОбъектаМД(ТекСоединениеВИБ, ИмяРеквизита, ОбъектМД));
```
</details>

## ВИБ_ЗаполнитьНедостающиеАналитики
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ЗаполнитьНедостающиеАналитики(ТекСоединениеВИБ, Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ЗаполнитьНедостающиеАналитики(ТекСоединениеВИБ, );
```
</details>

## ВИБ_ПолучитьОписаниеТиповБулево
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Служебная функция, предназначенная для получения описания типов булево,//
// Возвращаемое значение:
//  Объект "ОписаниеТипов"
//
Функция ВИБ_ПолучитьОписаниеТиповБулево(База) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьОписаниеТиповБулево(База) 
```
</details>

## ВИБ_ПолучитьОписаниеТиповСсылка
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ПолучитьОписаниеТиповСсылка(База,Описание) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьОписаниеТиповСсылка(База, Описание));
```
</details>

## ВИБ_ПолучитьЗначениеПеречисленияПоИмени
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ПолучитьЗначениеПеречисленияПоИмени(ТекСоединениеВИБ,ИмяОбъекта,ИмяПеречисления,НастройкаСоответствия=Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьЗначениеПеречисленияПоИмени(ТекСоединениеВИБ, ИмяОбъекта, ИмяПеречисления, НастройкаСоответствия);
```
</details>

## ВИБ_ПолучитьПустуюСсылкуПеречисление
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ВИБ_ПолучитьПустуюСсылкуПеречисление(ТекСоединениеВИБ,ИмяОбъекта) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьПустуюСсылкуПеречисление(ТекСоединениеВИБ, ИмяОбъекта));
```
</details>

## ВИБ_ПолучитьЗначениеПеречисленияПоНастройке
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ПолучитьЗначениеПеречисленияПоНастройке(ТекСоединениеВИБ,ИмяОбъекта,ИмяПеречисления,НастройкаСоответствия) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьЗначениеПеречисленияПоНастройке(ТекСоединениеВИБ, ИмяОбъекта, ИмяПеречисления, НастройкаСоответствия));
```
</details>

## ВИБ_ПолучитьЭлементАналитики
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция  ВИБ_ПолучитьЭлементАналитики(СтрДанные, Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьЭлементАналитики(СтрДанные, );
```
</details>

## ВИБ_ОбновитьДанныеОбъекта
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ОбновитьДанныеОбъекта(Ссылка,СтруктураОбновления,ТекСоединениеВИБ,ОбновлятьРеквизитыПриИмпорте=Ложь,МассивОшибок) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ОбновитьДанныеОбъекта(Ссылка, СтруктураОбновления, ТекСоединениеВИБ, ОбновлятьРеквизитыПриИмпорте, МассивОшибок));
```
</details>

## ВИБ_НеИспользоватьДляСинхронизацииПодчиненный
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_НеИспользоватьДляСинхронизацииПодчиненный(ИмяРеквизита,НастройкаСоответствияРеквизит,НеИспользоватьДляСинхронизацииТек) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_НеИспользоватьДляСинхронизацииПодчиненный(ИмяРеквизита, НастройкаСоответствияРеквизит, НеИспользоватьДляСинхронизацииТек));
```
</details>

## ВИБ_ПолучитьЗначениеПодчиненногоРеквизита
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ВИБ_ПолучитьЗначениеПодчиненногоРеквизита(СтрДанные, Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьЗначениеПодчиненногоРеквизита(СтрДанные, );
```
</details>

## СкопироватьУниверсальнуюКоллекцию
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Создает копию переданного массива, структуры или соответствия
//
// Параметры:
//  КоллекцияИсходная: тип Массив, Структура, Соответствие - исходная коллекция
// Возвращаемое значение:
//  КоллекцияКопия:  тип Массив, Структура, Соответствие - копия исходной коллекции
//
Функция СкопироватьУниверсальнуюКоллекцию(КоллекцияИсходная) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.СкопироватьУниверсальнуюКоллекцию(КоллекцияИсходная) 
```
</details>

## ВИБ_ПолучитьОбъектВнешнейИБПоGUIDТекущей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВИБ_ПолучитьОбъектВнешнейИБПоGUIDТекущей(ДанныеРеквизитов,КэшНайденныхЗначений,ТекСоединениеВИБ,ОбъектТекущейИБ,МассивОшибок) Экспорт
```

Пример вызова
```bsl
Результат = РаботаСОбъектамиМетаданныхВнешнийУХ.ВИБ_ПолучитьОбъектВнешнейИБПоGUIDТекущей(ДанныеРеквизитов, КэшНайденныхЗначений, ТекСоединениеВИБ, ОбъектТекущейИБ, МассивОшибок));
```
</details>

---

© 2024, **ООО 1С-Софт**  
Все права защищены. Эта программа и сопроводительные материалы предоставляются  
в соответствии с условиями лицензии [Attribution 4.0 International (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/legalcode).

---
