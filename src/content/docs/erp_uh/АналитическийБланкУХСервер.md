---
title: Аналитический бланк УХ сервер
slug: erp_uh/analiticheskiy-blank-uh-server
---


## ПодготовитьТекстЗапросаПолученияДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПодготовитьТекстЗапросаПолученияДанных(ОбъектАБ,ПолеТабличногоДокументаМакет,СтруктураАналитикРаскрытияПоказателей) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПодготовитьТекстЗапросаПолученияДанных(ОбъектАБ, ПолеТабличногоДокументаМакет, СтруктураАналитикРаскрытияПоказателей) 
```
</details>

## ПодготовитьТекстЗапросаСинтетика
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПодготовитьТекстЗапросаСинтетика(СтруктураПараметров,ОбъектАБ,ПолеТабличногоДокументаМакет) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПодготовитьТекстЗапросаСинтетика(СтруктураПараметров, ОбъектАБ, ПолеТабличногоДокументаМакет));
```
</details>

## ЗаполнитьТаблицуОтборовДопАналитик
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗаполнитьТаблицуОтборовДопАналитик(УидГруппыОтборов,ТзДоступныхАналитик=Неопределено,ОбъектАБ,ТаблицаТекущихОтборов = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ЗаполнитьТаблицуОтборовДопАналитик(УидГруппыОтборов, ТзДоступныхАналитик, ОбъектАБ, ТаблицаТекущихОтборов);
```
</details>

## СоздатьТаблицуПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СоздатьТаблицуПоказателей(ФинальныйЗапрос) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.СоздатьТаблицуПоказателей(ФинальныйЗапрос));
```
</details>

## СоздатьТаблицуЛогаНечисловых
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СоздатьТаблицуЛогаНечисловых(ФинальныйЗапрос, УИДГруппы) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.СоздатьТаблицуЛогаНечисловых(ФинальныйЗапрос, УИДГруппы));
```
</details>

## ПолучитьПодзапросТекущейВерсии
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьПодзапросТекущейВерсии(ФинальныйЗапрос,СтрГруппы,ТзПараметровЗапроса,ОбъектАБ,ИндексРегистра,ТаблицаТекущихОтборов) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьПодзапросТекущейВерсии(ФинальныйЗапрос, СтрГруппы, ТзПараметровЗапроса, ОбъектАБ, ИндексРегистра, ТаблицаТекущихОтборов));
```
</details>

## СформироватьТекстОтбора
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СформироватьТекстОтбора(ПрефиксТаблицы,ПрефиксПараметра,ТаблицаТекущихОтборов,ТзПараметровЗапроса,ИмяПоля ="",ОператорОбъединения=" И ",ИдентификаторРодителя=0) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.СформироватьТекстОтбора(ПрефиксТаблицы, ПрефиксПараметра, ТаблицаТекущихОтборов, ТзПараметровЗапроса, ИмяПоля, ОператорОбъединения, ИдентификаторРодителя);
```
</details>

## ПолучитьТекстОтбора
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТекстОтбора(ПрефиксТаблицы,ПрефиксПараметра,СтрокаОтбора="ПериодОтчета",ТзПараметровЗапроса,нИмяПоля="",ОператорОбъединения) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьТекстОтбора(ПрефиксТаблицы, ПрефиксПараметра, СтрокаОтбора, ТзПараметровЗапроса, нИмяПоля, ОператорОбъединения) 
```
</details>

## ПолучитьТекстЗначенийПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьТекстЗначенийПоказателей(ТекстШаблонаЗапроса,УИДГруппы,ИндексРегистра,ТзПараметровЗапроса,глТзАналитик,ГруппаПоказателей=Неопределено,ИндексЗапроса,ОбъектАБ,МаксИспользуемыхАналитик,ТаблицаТекущихОтборов) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьТекстЗначенийПоказателей(ТекстШаблонаЗапроса, УИДГруппы, ИндексРегистра, ТзПараметровЗапроса, глТзАналитик, ГруппаПоказателей, ИндексЗапроса, ОбъектАБ, МаксИспользуемыхАналитик, ТаблицаТекущихОтборов));
```
</details>

## ПолучитьТекстЗначенийДанныхЛога
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьТекстЗначенийДанныхЛога(ТекстШаблонаЗапроса,УИДГруппы,ИндексРегистра,ТзПараметровЗапроса,глТзАналитик,ГруппаПоказателей=Неопределено,ИндексЗапроса,ОбъектАБ,МаксИспользуемыхАналитик,ТаблицаТекущихОтборов) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьТекстЗначенийДанныхЛога(ТекстШаблонаЗапроса, УИДГруппы, ИндексРегистра, ТзПараметровЗапроса, глТзАналитик, ГруппаПоказателей, ИндексЗапроса, ОбъектАБ, МаксИспользуемыхАналитик, ТаблицаТекущихОтборов));
```
</details>

## ПолучитьТекстКомментариевЗначенийПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьТекстКомментариевЗначенийПоказателей(ТекстШаблонаЗапроса,УИДГруппы,ИндексРегистра,ТзПараметровЗапроса,глТзАналитик,ГруппаПоказателей=Неопределено,ИндексЗапроса,ОбъектАБ,МаксИспользуемыхАналитик) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьТекстКомментариевЗначенийПоказателей(ТекстШаблонаЗапроса, УИДГруппы, ИндексРегистра, ТзПараметровЗапроса, глТзАналитик, ГруппаПоказателей, ИндексЗапроса, ОбъектАБ, МаксИспользуемыхАналитик));
```
</details>

## ПодготовитьТекстФинальногоЗапросаДанные
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПодготовитьТекстФинальногоЗапросаДанные(ТекстИтоговогоЗапроса) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПодготовитьТекстФинальногоЗапросаДанные(ТекстИтоговогоЗапроса));
```
</details>

## ПодготовитьТекстФинальногоЗапросаСинтетика
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПодготовитьТекстФинальногоЗапросаСинтетика(ТзПараметровЗапроса,ПоказателиСинтетика) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПодготовитьТекстФинальногоЗапросаСинтетика(ТзПараметровЗапроса, ПоказателиСинтетика));
```
</details>

## СформироватьТекстОтбораПоВерсии
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СформироватьТекстОтбораПоВерсии(ПрефиксТаблицы,ПрефиксПараметра,СтрокаОтбора="ПериодОтчета",ТзПараметровЗапроса,нИмяПоля="") Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.СформироватьТекстОтбораПоВерсии(ПрефиксТаблицы, ПрефиксПараметра, СтрокаОтбора, ТзПараметровЗапроса, нИмяПоля);
```
</details>

## ПоддерживаетИерархиюГрупп
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция  ПоддерживаетИерархиюГрупп(ТипСтрока) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПоддерживаетИерархиюГрупп(ТипСтрока));
```
</details>

## ЗаполнитьИерархиюАналитикРаскрываемойОбласти
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗаполнитьИерархиюАналитикРаскрываемойОбласти(ОбластьРаскрытия,ТипОбласти,тзОписаниеАналитик,глТзАналитик,ОбъектАБ,ПолеТабличногоДокументаМакет,тСтруктураМакетовРаскрытия = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ЗаполнитьИерархиюАналитикРаскрываемойОбласти(ОбластьРаскрытия, ТипОбласти, тзОписаниеАналитик, глТзАналитик, ОбъектАБ, ПолеТабличногоДокументаМакет, тСтруктураМакетовРаскрытия);
```
</details>

## ПолучитьТекстЗапросаМакетаПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьТекстЗапросаМакетаПоказателей(ТзАналитик,НастройкиРаскрываемыхОбластей,ФинальныйЗапрос,ОписаниеТекущейГруппыОтборовАналитикРаскрытия,ТзПараметровЗапроса,ОписаниеТекущейОбласти,ТзПоказателейОбластей,нТзПараметровОбластей = Неопределено) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьТекстЗапросаМакетаПоказателей(ТзАналитик, НастройкиРаскрываемыхОбластей, ФинальныйЗапрос, ОписаниеТекущейГруппыОтборовАналитикРаскрытия, ТзПараметровЗапроса, ОписаниеТекущейОбласти, ТзПоказателейОбластей, нТзПараметровОбластей);
```
</details>

## СформироватьТекстДопПолей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СформироватьТекстДопПолей(СтрАналитика,нТзПараметровОбластей,ТекстДопПолейФинальногоЗапросаПолей) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.СформироватьТекстДопПолей(СтрАналитика, нТзПараметровОбластей, ТекстДопПолейФинальногоЗапросаПолей));
```
</details>

## ПолучитьТекстЗначенийПоказателей_Раскрытие
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьТекстЗначенийПоказателей_Раскрытие(ТекстШаблонаЗапроса,СтрГруппы,ИндексРегистра,ОписаниеТекущейОбласти,АналитикиРаскрытия,глТзАналитик,ГруппаПоказателей,ИндексЗапроса,ТзПараметровЗапроса,ОбъектАБ,ТзПараметровОбластей = Неопределено) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьТекстЗначенийПоказателей_Раскрытие(ТекстШаблонаЗапроса, СтрГруппы, ИндексРегистра, ОписаниеТекущейОбласти, АналитикиРаскрытия, глТзАналитик, ГруппаПоказателей, ИндексЗапроса, ТзПараметровЗапроса, ОбъектАБ, ТзПараметровОбластей);
```
</details>

## ПолучитьТипОбласти
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТипОбласти(ПолеТабличногоДокументаМакет,ИмяОбласти) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьТипОбласти(ПолеТабличногоДокументаМакет, ИмяОбласти));
```
</details>

## СформироватьТекстДополнительныхПолейЗапроса
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СформироватьТекстДополнительныхПолейЗапроса(СтруктураПараметров,ТекстАналитик,ТекстДополнительногоСоединения,ЗаменятьПоляВыборки = Ложь,СтруктураИндексовАналитик) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.СформироватьТекстДополнительныхПолейЗапроса(СтруктураПараметров, ТекстАналитик, ТекстДополнительногоСоединения, ЗаменятьПоляВыборки, СтруктураИндексовАналитик));
```
</details>

## ПолучитьСвойстваПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция  ПолучитьСвойстваПоказателей(мПоказатели) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьСвойстваПоказателей(мПоказатели) 
```
</details>

## ПреобразоватьОтборСКДВСтроку
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПреобразоватьОтборСКДВСтроку(нСтр,СтрОтбор) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПреобразоватьОтборСКДВСтроку(нСтр, СтрОтбор) 
```
</details>

## СформироватьЗапросПолученияТаблицыДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СформироватьЗапросПолученияТаблицыДанных(глСтруктураПараметров) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.СформироватьЗапросПолученияТаблицыДанных(глСтруктураПараметров) 
```
</details>

## ПодготовитьТекстЗапросаРаскрытияКолонки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

//Колонки без пересечений
Процедура ПодготовитьТекстЗапросаРаскрытияКолонки(СтруктураПараметров,ОбъектАБ,ПолеТабличногоДокументаМакет); Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПодготовитьТекстЗапросаРаскрытияКолонки(СтруктураПараметров, ОбъектАБ, ПолеТабличногоДокументаМакет););
```
</details>

## ПолучитьКэшГруппОтборовПоказателейРаскрытияКолонки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьКэшГруппОтборовПоказателейРаскрытияКолонки(СтруктураПараметров) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьКэшГруппОтборовПоказателейРаскрытияКолонки(СтруктураПараметров));
```
</details>

## ПолучитьОписаниеОбластиКолонок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьОписаниеОбластиКолонок(СтруктураПараметров) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьОписаниеОбластиКолонок(СтруктураПараметров));
```
</details>

## ПолучитьТекстМакетаСгруппировкамиКолонки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьТекстМакетаСгруппировкамиКолонки(ТекстИтоговогоЗапроса,глТзАналитик,ТзПараметровОбластей,ПоказателиКолонки,ТзПараметровЗапроса) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьТекстМакетаСгруппировкамиКолонки(ТекстИтоговогоЗапроса, глТзАналитик, ТзПараметровОбластей, ПоказателиКолонки, ТзПараметровЗапроса));
```
</details>

## ПолучитьТекстМакетаСгруппировкамиКолонкиСКД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьТекстМакетаСгруппировкамиКолонкиСКД(ТекстИтоговогоЗапроса,глТзАналитик,ТзПараметровОбластей,ПоказателиКолонки,ТзПараметровЗапроса) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьТекстМакетаСгруппировкамиКолонкиСКД(ТекстИтоговогоЗапроса, глТзАналитик, ТзПараметровОбластей, ПоказателиКолонки, ТзПараметровЗапроса));
```
</details>

## ПолучитьТекстЗапросаВыводаМакетаКолонкиСКД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТекстЗапросаВыводаМакетаКолонкиСКД(ПоказателиТекущегоЗапроса,ТзАналитик,СтруктураВыводаТекущейОбласти,Уид_Области,ТзПараметровОбластей,КэшУникальныхКодовПоказателей) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьТекстЗапросаВыводаМакетаКолонкиСКД(ПоказателиТекущегоЗапроса, ТзАналитик, СтруктураВыводаТекущейОбласти, Уид_Области, ТзПараметровОбластей, КэшУникальныхКодовПоказателей));
```
</details>

## ПолучитьТекстЗапросаВыводаМакетаКолонки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТекстЗапросаВыводаМакетаКолонки(ПоказателиТекущегоЗапроса,ТзАналитик,СтруктураВыводаТекущейОбласти,Уид_Области,ТзПараметровОбластей,ПоказателиКолонки,КэшУникальныхКодовПоказателей) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьТекстЗапросаВыводаМакетаКолонки(ПоказателиТекущегоЗапроса, ТзАналитик, СтруктураВыводаТекущейОбласти, Уид_Области, ТзПараметровОбластей, ПоказателиКолонки, КэшУникальныхКодовПоказателей));
```
</details>

## ПолучитьОписаниеСхемыКДВыводаМакетКолонки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьОписаниеСхемыКДВыводаМакетКолонки(СтруктураПараметров) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьОписаниеСхемыКДВыводаМакетКолонки(СтруктураПараметров));
```
</details>

## ПодготовитьТекстЗапросаРаскрытияСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

//Строки без пересечений
Процедура ПодготовитьТекстЗапросаРаскрытияСтроки(СтруктураПараметров,ОбъектАБ,ПолеТабличногоДокументаМакет); Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПодготовитьТекстЗапросаРаскрытияСтроки(СтруктураПараметров, ОбъектАБ, ПолеТабличногоДокументаМакет););
```
</details>

## ПолучитьКэшГруппОтборовПоказателейРаскрытияСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьКэшГруппОтборовПоказателейРаскрытияСтроки(СтруктураПараметров) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьКэшГруппОтборовПоказателейРаскрытияСтроки(СтруктураПараметров));
```
</details>

## ПолучитьОписаниеОбластиСтрок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьОписаниеОбластиСтрок(СтруктураПараметров) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьОписаниеОбластиСтрок(СтруктураПараметров));
```
</details>

## ПолучитьТекстМакетаСгруппировкамиСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьТекстМакетаСгруппировкамиСтроки(ТекстИтоговогоЗапроса,глТзАналитик,ТзПараметровОбластей,ПоказателиТекущейОбласти,ТзПараметровЗапроса) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьТекстМакетаСгруппировкамиСтроки(ТекстИтоговогоЗапроса, глТзАналитик, ТзПараметровОбластей, ПоказателиТекущейОбласти, ТзПараметровЗапроса));
```
</details>

## ПолучитьТекстЗапросаВыводаМакетаСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТекстЗапросаВыводаМакетаСтроки(ПоказателиТекущегоЗапроса,ТзАналитик,СтруктураВыводаТекущейОбласти,Уид_Области,ТзПараметровОбластей,КэшУникальныхКодовПоказателей) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьТекстЗапросаВыводаМакетаСтроки(ПоказателиТекущегоЗапроса, ТзАналитик, СтруктураВыводаТекущейОбласти, Уид_Области, ТзПараметровОбластей, КэшУникальныхКодовПоказателей));
```
</details>

## ПолучитьМассивИзСтруктуры
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьМассивИзСтруктуры(СтруктураИсточник) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьМассивИзСтруктуры(СтруктураИсточник));
```
</details>

## ЗаполнитьКэшПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнитьКэшПоказателей(ТзАналитик,ПоказателиТекущейГруппы,ГлКэшПоказателей_,ГлКэшАналитик_,КэшПоказателей,КэшАналитик) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ЗаполнитьКэшПоказателей(ТзАналитик, ПоказателиТекущейГруппы, ГлКэшПоказателей_, ГлКэшАналитик_, КэшПоказателей, КэшАналитик));
```
</details>

## ПолучитьПараметрыТекущейОбласти
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПараметрыТекущейОбласти(СтрГруппаУид,ТзПараметровОбластей,тСтруктураГруппирвок) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьПараметрыТекущейОбласти(СтрГруппаУид, ТзПараметровОбластей, тСтруктураГруппирвок));
```
</details>

## ОбластьСодержитМакет
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ОбластьСодержитМакет(ТзАналитик) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ОбластьСодержитМакет(ТзАналитик));
```
</details>

## ПолучитьОписаниеСхемыКДВыводаМакетСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьОписаниеСхемыКДВыводаМакетСтроки(СтруктураПараметров) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьОписаниеСхемыКДВыводаМакетСтроки(СтруктураПараметров));
```
</details>

## ЕстьПолеСКД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЕстьПолеСКД(Коллекция,КодПоля) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ЕстьПолеСКД(Коллекция, КодПоля));
```
</details>

## ЭтоСоставнойТип
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЭтоСоставнойТип(ТзАналитик,КодАналитики) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ЭтоСоставнойТип(ТзАналитик, КодАналитики));
```
</details>

## ПодготовитьТекстЗапросаРаскрытияТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

//Таблицы
Процедура ПодготовитьТекстЗапросаРаскрытияТаблицы(СтруктураПараметров,ОбъектАБ,ПолеТабличногоДокументаМакет); Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПодготовитьТекстЗапросаРаскрытияТаблицы(СтруктураПараметров, ОбъектАБ, ПолеТабличногоДокументаМакет););
```
</details>

## ПолучитьКэшГруппОтборовПоказателейРаскрытияТаблиц
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьКэшГруппОтборовПоказателейРаскрытияТаблиц(СтруктураПараметров) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьКэшГруппОтборовПоказателейРаскрытияТаблиц(СтруктураПараметров));
```
</details>

## ЗаполнитьИерархиюАналитикРаскрываемойОбластиТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗаполнитьИерархиюАналитикРаскрываемойОбластиТаблицы(ОбластьРаскрытия,ТипОбласти,тзОписаниеАналитик,глТзАналитик,ОбъектАБ,РаскрываемаяОбласть,тСтруктураМакетовРаскрытия) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ЗаполнитьИерархиюАналитикРаскрываемойОбластиТаблицы(ОбластьРаскрытия, ТипОбласти, тзОписаниеАналитик, глТзАналитик, ОбъектАБ, РаскрываемаяОбласть, тСтруктураМакетовРаскрытия));
```
</details>

## ПолучитьОписаниеОбластиТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьОписаниеОбластиТаблицы(СтруктураПараметров) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьОписаниеОбластиТаблицы(СтруктураПараметров));
```
</details>

## ПолучитьТекстЗапросаВыводаИтоговТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТекстЗапросаВыводаИтоговТаблицы(ПоказателиТекущегоЗапроса,ТзАналитик,СтруктураВыводаТекущейОбласти,Уид_Области,ТзПараметровОбластей,КэшУникальныхКодовПоказателей,ИндексЗапроса,УидОбласти) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьТекстЗапросаВыводаИтоговТаблицы(ПоказателиТекущегоЗапроса, ТзАналитик, СтруктураВыводаТекущейОбласти, Уид_Области, ТзПараметровОбластей, КэшУникальныхКодовПоказателей, ИндексЗапроса, УидОбласти));
```
</details>

## ДополнитьЗапросИтогамиПоИерархическимСправочникам
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ДополнитьЗапросИтогамиПоИерархическимСправочникам(ТзАналитик,ТзПараметровОбластей,СтруктураВыводаТекущейОбласти) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ДополнитьЗапросИтогамиПоИерархическимСправочникам(ТзАналитик, ТзПараметровОбластей, СтруктураВыводаТекущейОбласти));
```
</details>

## ПолучитьТекстЗначенийПоказателейТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьТекстЗначенийПоказателейТаблицы(ТекстШаблонаЗапроса,СтрГруппы,ИндексРегистра,ОписаниеТекущейОбласти,АналитикиРаскрытия,глТзАналитик,ГруппаПоказателей,ИндексЗапроса,ТзПараметровЗапроса,ОбъектАБ) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьТекстЗначенийПоказателейТаблицы(ТекстШаблонаЗапроса, СтрГруппы, ИндексРегистра, ОписаниеТекущейОбласти, АналитикиРаскрытия, глТзАналитик, ГруппаПоказателей, ИндексЗапроса, ТзПараметровЗапроса, ОбъектАБ));
```
</details>

## ПолучитьШиринуМакетКолонокДляВыводаОбласти
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьШиринуМакетКолонокДляВыводаОбласти(тМакет,ИмяМакета,ИмяМакетаГруппы,РаскрываемыеОбластиКолонки) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьШиринуМакетКолонокДляВыводаОбласти(тМакет, ИмяМакета, ИмяМакетаГруппы, РаскрываемыеОбластиКолонки));
```
</details>

## ПолучитьВысотуМакетКолонокДляВыводаОбласти
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьВысотуМакетКолонокДляВыводаОбласти(тМакет,ИмяМакета,ИмяМакетаГруппы,РаскрываемыеОбластиСтроки) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьВысотуМакетКолонокДляВыводаОбласти(тМакет, ИмяМакета, ИмяМакетаГруппы, РаскрываемыеОбластиСтроки));
```
</details>

## ПолучитьОписаниеСхемыКДВыводаТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьОписаниеСхемыКДВыводаТаблицы(СтруктураПараметров); Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьОписаниеСхемыКДВыводаТаблицы(СтруктураПараметров););
```
</details>

## ОбластьВходитВТаблицу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ОбластьВходитВТаблицу(ИндексСтроки,ИндексКолонки,ТипОбласти,ОбъектАБ,ПолеТабличногоДокументаМакет,тСоставОбластейРаскрытия = Неопределено,ИмяОбласти = "") Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ОбластьВходитВТаблицу(ИндексСтроки, ИндексКолонки, ТипОбласти, ОбъектАБ, ПолеТабличногоДокументаМакет, тСоставОбластейРаскрытия, ИмяОбласти);
```
</details>

## ОбработатьСтрокуШаблонаЗаголовкаАналитики
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ОбработатьСтрокуШаблонаЗаголовкаАналитики(ИсходнаяСтрока,ДоступныеПоля) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ОбработатьСтрокуШаблонаЗаголовкаАналитики(ИсходнаяСтрока, ДоступныеПоля));
```
</details>

## ПолучитьСвойстваПоказателя
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСвойстваПоказателя(Расшифровка,тПоказатель) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьСвойстваПоказателя(Расшифровка, тПоказатель));
```
</details>

## ПроверитьВыводЗначенияПоПоказателю
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПроверитьВыводЗначенияПоПоказателю(СтруктураПараметров) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПроверитьВыводЗначенияПоПоказателю(СтруктураПараметров));
```
</details>

## ДобавитьОписаниеПоляСКД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьОписаниеПоляСКД(ИмяПоля,СинонимПоля,ТипДанных,МакетСкд) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ДобавитьОписаниеПоляСКД(ИмяПоля, СинонимПоля, ТипДанных, МакетСкд) 
```
</details>

## ПолучитьКэшПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьКэшПоказателей(МассивПоказателей,ТзАналитикИсходная,КэшПоказателей,КэшАналитик) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьКэшПоказателей(МассивПоказателей, ТзАналитикИсходная, КэшПоказателей, КэшАналитик));
```
</details>

## ПолучитьТаблицыТекущегоЭкземпляраОтчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьТаблицыТекущегоЭкземпляраОтчета(РабочийОбъект) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьТаблицыТекущегоЭкземпляраОтчета(РабочийОбъект) 
```
</details>

## ПолучитьПериодПоСдвигу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПолучитьПериодПоСдвигу(ДатаНачала,ОтборС,Периодичность) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьПериодПоСдвигу(ДатаНачала, ОтборС, Периодичность) 
```
</details>

## ПолучитьДоступныеПериодыНачалаПоСценарию
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьДоступныеПериодыНачалаПоСценарию(Сценарий) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьДоступныеПериодыНачалаПоСценарию(Сценарий) 
```
</details>

## ПолучитьДоступныеПериодыОкончанияПоСценарию
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьДоступныеПериодыОкончанияПоСценарию(Сценарий,ПериодОтчета) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьДоступныеПериодыОкончанияПоСценарию(Сценарий, ПериодОтчета) 
```
</details>

## ПолучитьКэшАналитикПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьКэшАналитикПоказателей(Показатели,ВидОтчета=Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьКэшАналитикПоказателей(Показатели, ВидОтчета);
```
</details>

## ЗаписатьЗначениеВПоказатель
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаписатьЗначениеВПоказатель(ТабИзмененныхДанных,мПоказателиРаскрытия = Неопределено,Значение,СписокАналитик,КэшАналитикПоказателей,ФлагОшибки=Ложь,ЕдиницаИзмерения) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ЗаписатьЗначениеВПоказатель(ТабИзмененныхДанных, мПоказателиРаскрытия, Значение, СписокАналитик, КэшАналитикПоказателей, ФлагОшибки, ЕдиницаИзмерения) 
```
</details>

## ПолучитьПредставлениеПериодаСервер
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПредставлениеПериодаСервер(Периодичность,ПериодНачала,ПериодОкончания) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьПредставлениеПериодаСервер(Периодичность, ПериодНачала, ПериодОкончания) 
```
</details>

## ЗаписатьКомментарийВПоказатель
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаписатьКомментарийВПоказатель(ТабИзмененныхДанных,мПоказателиРаскрытия = Неопределено,ТекстКомментария,СписокАналитик,КэшАналитикПоказателей,ФлагОшибки=Ложь) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ЗаписатьКомментарийВПоказатель(ТабИзмененныхДанных, мПоказателиРаскрытия, ТекстКомментария, СписокАналитик, КэшАналитикПоказателей, ФлагОшибки);
```
</details>

## ОчиститьДанныеТекущегоЭкземпляраОтчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОчиститьДанныеТекущегоЭкземпляраОтчета(ВидОтчета,Сценарий,Периоды,Организация,Проект,ДопОтборы,ЛогИзмененныхДанныхАдрес,ИдентификаторСеанса) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ОчиститьДанныеТекущегоЭкземпляраОтчета(ВидОтчета, Сценарий, Периоды, Организация, Проект, ДопОтборы, ЛогИзмененныхДанныхАдрес, ИдентификаторСеанса) 
```
</details>

## ОбработатьИзменениеКлючейВерсии
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОбработатьИзменениеКлючейВерсии(Ссылка,мКопируемыйДокумент,СтруктураТекущихКлючевыхРеквизитов) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ОбработатьИзменениеКлючейВерсии(Ссылка, мКопируемыйДокумент, СтруктураТекущихКлючевыхРеквизитов) 
```
</details>

## УдалитьПромежуточныеВерсии
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УдалитьПромежуточныеВерсии(Параметры, АдресХранилища, АдресХранилищаДополнительный) ЭКспорт Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.УдалитьПромежуточныеВерсии(Параметры, АдресХранилища, АдресХранилищаДополнительный) ЭКспорт);
```
</details>

## ВыполнитьРеструктуризациюБланков
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВыполнитьРеструктуризациюБланков(Ссылка,АдресПараметровРеструктуризации) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ВыполнитьРеструктуризациюБланков(Ссылка, АдресПараметровРеструктуризации) 
```
</details>

## ДополнитьПараметрыОбластейПолямиПорядка
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДополнитьПараметрыОбластейПолямиПорядка(тПараметрыОбластей,ТзАналитик) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ДополнитьПараметрыОбластейПолямиПорядка(тПараметрыОбластей, ТзАналитик));
```
</details>

## ЗаполнитьШаблонМакетаДляВыгрузки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнитьШаблонМакетаДляВыгрузки(Шаблон, ИсходныйМакет, БланкОтчета, Периодичность = Неопределено, КоличествоПериодов = Неопределено, ВыгрузкаМакетаДляEXCEL = Ложь, ОбъектРасчета = Неопределено) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ЗаполнитьШаблонМакетаДляВыгрузки(Шаблон, ИсходныйМакет, БланкОтчета, Периодичность, КоличествоПериодов, ВыгрузкаМакетаДляEXCEL, ОбъектРасчета);
```
</details>

## ПолучитьШаблонМакетаДляВыгрузки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьШаблонМакетаДляВыгрузки(ИсходныйМакет,БланкОтчета,Периодичность=Неопределено,КоличествоПериодов=Неопределено,ВыгрузкаМакетаДляEXCEL = Ложь,ОбъектРасчета = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьШаблонМакетаДляВыгрузки(ИсходныйМакет, БланкОтчета, Периодичность, КоличествоПериодов, ВыгрузкаМакетаДляEXCEL, ОбъектРасчета);
```
</details>

## ПолучитьСтруктуруРасшифровки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСтруктуруРасшифровки(ПолеТабличногоДокументаМакет,Объект,ОбъектАналитческогоБланка,ОбластьИмя,Расшифровка,СтруктураНезаполненыхАналитик,СтруктураОтбираемыхАналитик,ТзСвойстваПоказателей) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьСтруктуруРасшифровки(ПолеТабличногоДокументаМакет, Объект, ОбъектАналитческогоБланка, ОбластьИмя, Расшифровка, СтруктураНезаполненыхАналитик, СтруктураОтбираемыхАналитик, ТзСвойстваПоказателей) 
```
</details>

## ПолучитьСтруктуруРасшифровкиБезКонтекста
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьСтруктуруРасшифровкиБезКонтекста(Объект,СтруктураРасшифровки,СтруктураНезаполненыхАналитик,СтруктураОтбираемыхАналитик,РасшифровкаГруппОтборов,тПоказатель) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьСтруктуруРасшифровкиБезКонтекста(Объект, СтруктураРасшифровки, СтруктураНезаполненыхАналитик, СтруктураОтбираемыхАналитик, РасшифровкаГруппОтборов, тПоказатель) 
```
</details>

## ВычислитьТекущиеЗначенияРасшифровки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВычислитьТекущиеЗначенияРасшифровки(Объект,РасшифровкаГруппОтборов,СтруктураРасшифровки,УидГруппыОтборов,тПоказатель) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ВычислитьТекущиеЗначенияРасшифровки(Объект, РасшифровкаГруппОтборов, СтруктураРасшифровки, УидГруппыОтборов, тПоказатель));
```
</details>

## ПолучитьПолнуюСтруктуруАналитик
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПолнуюСтруктуруАналитик(тПоказатель) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьПолнуюСтруктуруАналитик(тПоказатель) 
```
</details>

## ПолучитьСвойстваПоказателяСтруктура
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьСвойстваПоказателяСтруктура(ТзСвойстваПоказателей,Расшифровка,СтруктураРасшифровки,тПоказатель) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ПолучитьСвойстваПоказателяСтруктура(ТзСвойстваПоказателей, Расшифровка, СтруктураРасшифровки, тПоказатель) 
```
</details>

## ВставитьЗначениеВОбластьТабличногоДокумента
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВставитьЗначениеВОбластьТабличногоДокумента(ОбластьТабличногоДокумента, ЗначениеЯчейки = Неопределено, ФорматЯчейки = Неопределено, ТочностьЧисла = 0, СоставДаты = неопределено) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ВставитьЗначениеВОбластьТабличногоДокумента(ОбластьТабличногоДокумента, ЗначениеЯчейки, ФорматЯчейки, ТочностьЧисла, СоставДаты);
```
</details>

## ПолучитьЗначениеОбластиТабличногоДокумента
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьЗначениеОбластиТабличногоДокумента(ОбластьТабличногоДокумента) Экспорт
```

Пример вызова
```bsl
Результат = АналитическийБланкУХСервер.ПолучитьЗначениеОбластиТабличногоДокумента(ОбластьТабличногоДокумента) 
```
</details>

## ВернутьИмяФормыАналитическогоБланка
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВернутьИмяФормыАналитическогоБланка(ИмяФормы) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.ВернутьИмяФормыАналитическогоБланка(ИмяФормы) 
```
</details>

## СкопироватьНастройкиБланка
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура  СкопироватьНастройкиБланка(Выборка_Ссылка, НовЭл_Ссылка) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.СкопироватьНастройкиБланка(Выборка_Ссылка, НовЭл_Ссылка) 
```
</details>

## РазобратьДанныеБуфера
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура РазобратьДанныеБуфера(ДанныеБуфера,МассивСтрок,МассивЗначений) Экспорт
```

Пример вызова
```bsl
АналитическийБланкУХСервер.РазобратьДанныеБуфера(ДанныеБуфера, МассивСтрок, МассивЗначений) 
```
</details>

---

© 2024, **ООО 1С-Софт**  
Все права защищены. Эта программа и сопроводительные материалы предоставляются  
в соответствии с условиями лицензии [Attribution 4.0 International (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/legalcode).

---
