---
title: Обмен с ГИС ЭПД клиент сервер
---


## ВерсияПрограммы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВерсияПрограммы() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ВерсияПрограммы() 
```
</details>

## ДанныеСертификатаУчетнойЗаписиЭДО
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ДанныеСертификатаУчетнойЗаписиЭДО(ИдентификаторУчетнойЗаписи, ОтборПодписанта = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ДанныеСертификатаУчетнойЗаписиЭДО(ИдентификаторУчетнойЗаписи, ОтборПодписанта);
```
</details>

## ПолучитьСообщениеТитула
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСообщениеТитула(Документ, ТипЭлементаРегламента) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьСообщениеТитула(Документ, ТипЭлементаРегламента) 
```
</details>

## ПолучитьАдресаДоставки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьАдресаДоставки(ЗначениеОтбора) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьАдресаДоставки(ЗначениеОтбора) 
```
</details>

## ИспользуютсяДополнительныеРеквизитыЭПД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИспользуютсяДополнительныеРеквизитыЭПД() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ИспользуютсяДополнительныеРеквизитыЭПД() 
```
</details>

## НоваяСтруктураДляРеестраЭПД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НоваяСтруктураДляРеестраЭПД() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.НоваяСтруктураДляРеестраЭПД() 
```
</details>

## ЗаполнениеСпискаСохраненныхАдресов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнениеСпискаСохраненныхАдресов(Форма, ЗначениеОтбора, Элемент, ДанныеВыбора = Неопределено, ЗаполнитьПоУмолчанию = Ложь) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ЗаполнениеСпискаСохраненныхАдресов(Форма, ЗначениеОтбора, Элемент, ДанныеВыбора, ЗаполнитьПоУмолчанию);
```
</details>

## ПрефиксыТитуловДокумента
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПрефиксыТитуловДокумента(ИмяДокумента) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПрефиксыТитуловДокумента(ИмяДокумента) 
```
</details>

## ПрефиксТитулаРеквизита
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПрефиксТитулаРеквизита(ИмяРеквизита, ИмяДокумента) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПрефиксТитулаРеквизита(ИмяРеквизита, ИмяДокумента) 
```
</details>

## ПроверитьСоответствиеТребованиямИНН
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПроверитьСоответствиеТребованиямИНН(Знач ИНН) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПроверитьСоответствиеТребованиямИНН(ИНН) 
```
</details>

## ПолучитьМассивТиповТС
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьМассивТиповТС() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьМассивТиповТС() 
```
</details>

## ПолучитьМассивТиповПрицепов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьМассивТиповПрицепов() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьМассивТиповПрицепов() 
```
</details>

## ПолучитьМассивМаркировокГруза
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПолучитьМассивМаркировокГруза() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьМассивМаркировокГруза() 
```
</details>

## УстановитьДоступностьЭлементовПоСостоянию
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура УстановитьДоступностьЭлементовПоСостоянию(Форма, ДанныеСостоянияЭДО, ЭтоНеЗаконченнаяОтправка) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьДоступностьЭлементовПоСостоянию(Форма, ДанныеСостоянияЭДО, ЭтоНеЗаконченнаяОтправка));
```
</details>

## УстановитьДоступностьЭлементовЭСВ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура УстановитьДоступностьЭлементовЭСВ(Форма) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьДоступностьЭлементовЭСВ(Форма) 
```
</details>

## УстановитьТекущийДоступныйТитулЭСВ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьТекущийДоступныйТитулЭСВ(Объект, ТитулыПоДокументу = Неопределено, ТребуетсяПовторно = Ложь) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьТекущийДоступныйТитулЭСВ(Объект, ТитулыПоДокументу, ТребуетсяПовторно);
```
</details>

## УстановитьДоступностьЭлементовЭТрН
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура УстановитьДоступностьЭлементовЭТрН(Форма) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьДоступностьЭлементовЭТрН(Форма) 
```
</details>

## УстановитьТекущийДоступныйТитулЭТрН
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура УстановитьТекущийДоступныйТитулЭТрН(Объект, ТитулыПоДокументу = Неопределено, ВерсииТитулов = Неопределено, ВводимДопТитул = Ложь, ТребуетсяПовторно = Ложь) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьТекущийДоступныйТитулЭТрН(Объект, ТитулыПоДокументу, ВерсииТитулов, ВводимДопТитул, ТребуетсяПовторно);
```
</details>

## УстановитьДоступностьЭлементовЭЗН
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура УстановитьДоступностьЭлементовЭЗН(Форма) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьДоступностьЭлементовЭЗН(Форма) 
```
</details>

## УстановитьТекущийДоступныйТитулЭЗН
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьТекущийДоступныйТитулЭЗН(Объект, ТитулыПоДокументу = Неопределено, ТребуетсяПовторно = Ложь) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьТекущийДоступныйТитулЭЗН(Объект, ТитулыПоДокументу, ТребуетсяПовторно);
```
</details>

## УстановитьДоступностьЭлементовЭЗЗ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура УстановитьДоступностьЭлементовЭЗЗ(Форма) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьДоступностьЭлементовЭЗЗ(Форма) 
```
</details>

## УстановитьТекущийДоступныйТитулЭЗЗ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьТекущийДоступныйТитулЭЗЗ(Объект, ТитулыПоДокументу = Неопределено, ТребуетсяПовторно = Ложь) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьТекущийДоступныйТитулЭЗЗ(Объект, ТитулыПоДокументу, ТребуетсяПовторно);
```
</details>

## УстановитьДоступностьЭлементовЭДФ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура УстановитьДоступностьЭлементовЭДФ(Форма) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьДоступностьЭлементовЭДФ(Форма) 
```
</details>

## УстановитьТекущийДоступныйТитулЭДФ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьТекущийДоступныйТитулЭДФ(Объект, ТитулыПоДокументу = Неопределено, ТребуетсяПовторно = Ложь) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьТекущийДоступныйТитулЭДФ(Объект, ТитулыПоДокументу, ТребуетсяПовторно);
```
</details>

## УстановитьДоступностьЭлементовЭПЛ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура УстановитьДоступностьЭлементовЭПЛ(Форма) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьДоступностьЭлементовЭПЛ(Форма) 
```
</details>

## УстановитьТекущийДоступныйТитулЭПЛ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьТекущийДоступныйТитулЭПЛ(Объект, Форма = Неопределено, ТитулыПоДокументу = Неопределено, ТребуетсяПовторно = Ложь, ВводимДопТитул = Ложь) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьТекущийДоступныйТитулЭПЛ(Объект, Форма, ТитулыПоДокументу, ТребуетсяПовторно, ВводимДопТитул);
```
</details>

## ВхождениеРоли
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ВхождениеРоли(РольУчастникаОбшая, РольУчастника, ВсегоРолей = 4) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ВхождениеРоли(РольУчастникаОбшая, РольУчастника, ВсегоРолей);
```
</details>

## ВсеВключающиеРоли
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВсеВключающиеРоли(РольУчастника, ВсегоРолей = 4) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ВсеВключающиеРоли(РольУчастника, ВсегоРолей);
```
</details>

## АдресатыПоИдФайл
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция АдресатыПоИдФайл(ИдФайл) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.АдресатыПоИдФайл(ИдФайл) 
```
</details>

## ПрефиксТитулаПоЭлементуРегламентаЭДО
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПрефиксТитулаПоЭлементуРегламентаЭДО(ЭлементРегламента) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПрефиксТитулаПоЭлементуРегламентаЭДО(ЭлементРегламента) 
```
</details>

## ПосчитатьКоличествСтрокВсехТаблиц
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПосчитатьКоличествСтрокВсехТаблиц(Форма) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ПосчитатьКоличествСтрокВсехТаблиц(Форма));
```
</details>

## ПосчитатьКоличествСтрокТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПосчитатьКоличествСтрокТаблицы(КонтейнерРеквизитов, СтраницаТаблица, КоличествоДобавить = 0) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ПосчитатьКоличествСтрокТаблицы(КонтейнерРеквизитов, СтраницаТаблица, КоличествоДобавить);
```
</details>

## ТаблицаВМассивСтруктур
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


// Параметры:
//   ТаблицаЗначений - ТаблицаЗначений - таблица значений для преобразования
//
Функция ТаблицаВМассивСтруктур(Знач ТаблицаЗначений) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ТаблицаВМассивСтруктур(ТаблицаЗначений) 
```
</details>

## ПреобразоватьВДату
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПреобразоватьВДату(Знач ДатаСтрока) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПреобразоватьВДату(ДатаСтрока) 
```
</details>

## ПреобразоватьВЧисло
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПреобразоватьВЧисло(Знач ЧислоСтрока) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПреобразоватьВЧисло(ЧислоСтрока) 
```
</details>

## ПреобразоватьВБулево
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПреобразоватьВБулево(Знач БулевоСтрока) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПреобразоватьВБулево(БулевоСтрока) 
```
</details>

## СкопироватьИзФиксированногоЗначенияРекурсивно
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Скопировать из фиксированного значения рекурсивно.
//
// Параметры:
//  Данные - Массив из Произвольный - Данные
//  	   - Структура - Данные
//
// Возвращаемое значение:
//  Структура, Массив из Произвольный -- Результат копирования
Функция СкопироватьИзФиксированногоЗначенияРекурсивно(Данные) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.СкопироватьИзФиксированногоЗначенияРекурсивно(Данные) 
```
</details>

## СкопироватьВФиксированноеЗначенияРекурсивно
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Скопировать в фиксированное значения рекурсивно.
//
// Параметры:
//  Данные - Структура, Массив из Произвольный - Данные
//
// Возвращаемое значение:
//  ФиксированныйМассив из Произвольный, ФиксированнаяСтруктура - Скопировать в фиксированное значения рекурсивно
Функция СкопироватьВФиксированноеЗначенияРекурсивно(Данные) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.СкопироватьВФиксированноеЗначенияРекурсивно(Данные) 
```
</details>

## ПолучитьЗначениеРеквизитаИзСтруктурыФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьЗначениеРеквизитаИзСтруктурыФормы(Форма, ИмяЭлемента, ЗначениеПоУмолчанию = Неопределено, КонтейнерОтображаемойВерсии = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьЗначениеРеквизитаИзСтруктурыФормы(Форма, ИмяЭлемента, ЗначениеПоУмолчанию, КонтейнерОтображаемойВерсии);
```
</details>

## УстановитьЗначениеРеквизитаВСтруктуреФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьЗначениеРеквизитаВСтруктуреФормы(Форма, ИмяЭлемента, Значение = Неопределено, Перезаписывать = Истина, КонтейнерОтображаемойВерсии = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьЗначениеРеквизитаВСтруктуреФормы(Форма, ИмяЭлемента, Значение, Перезаписывать, КонтейнерОтображаемойВерсии);
```
</details>

## УстановитьЗначениеПодчиненнойСтрокиВСтруктуреФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьЗначениеПодчиненнойСтрокиВСтруктуреФормы(Форма, ИмяТаблицы, ИдентификаторСтрокиРодителя, Значения, Перезаписывать = Истина, КонтейнерОтображаемойВерсии = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УстановитьЗначениеПодчиненнойСтрокиВСтруктуреФормы(Форма, ИмяТаблицы, ИдентификаторСтрокиРодителя, Значения, Перезаписывать, КонтейнерОтображаемойВерсии);
```
</details>

## ЗагрузитьТаблицуВСтруктуруФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗагрузитьТаблицуВСтруктуруФормы(Форма, ИмяТаблицы, Перезаписывать = Истина, КонтейнерОтображаемойВерсии = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ЗагрузитьТаблицуВСтруктуруФормы(Форма, ИмяТаблицы, Перезаписывать, КонтейнерОтображаемойВерсии);
```
</details>

## СкопироватьРеквизитыВСтруктуреФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СкопироватьРеквизитыВСтруктуреФормы(Форма, ПрефиксОткуда, ПрефиксКуда) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.СкопироватьРеквизитыВСтруктуреФормы(Форма, ПрефиксОткуда, ПрефиксКуда) 
```
</details>

## ПолучитьКоличествоСтрокТаблицыИзСтруктурыФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьКоличествоСтрокТаблицыИзСтруктурыФормы(Форма, ИмяТаблицы, КонтейнерОтображаемойВерсии = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьКоличествоСтрокТаблицыИзСтруктурыФормы(Форма, ИмяТаблицы, КонтейнерОтображаемойВерсии);
```
</details>

## СуммаПоСтрокамТаблицыИзСтруктурыФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СуммаПоСтрокамТаблицыИзСтруктурыФормы(Форма, ИмяТаблицы, ИмяКолонки, КонтейнерОтображаемойВерсии = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.СуммаПоСтрокамТаблицыИзСтруктурыФормы(Форма, ИмяТаблицы, ИмяКолонки, КонтейнерОтображаемойВерсии);
```
</details>

## СуммаПоСтрокамТаблицыИзСтруктурыРеквизитов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СуммаПоСтрокамТаблицыИзСтруктурыРеквизитов(СтруктураРеквизитов, ИмяТаблицы, ИмяКолонки) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.СуммаПоСтрокамТаблицыИзСтруктурыРеквизитов(СтруктураРеквизитов, ИмяТаблицы, ИмяКолонки) 
```
</details>

## УдалитьРеквизитыПоГруппеДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УдалитьРеквизитыПоГруппеДанных(Форма, ГруппаДанных, КонтейнерОтображаемойВерсии = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УдалитьРеквизитыПоГруппеДанных(Форма, ГруппаДанных, КонтейнерОтображаемойВерсии);
```
</details>

## УдалитьСтрокиТаблицыИзСтруктурыФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УдалитьСтрокиТаблицыИзСтруктурыФормы(Форма, ИмяТаблицы, НомерСтроки = Неопределено, КонтейнерОтображаемойВерсии = Неопределено, КромеУказанной = Ложь) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.УдалитьСтрокиТаблицыИзСтруктурыФормы(Форма, ИмяТаблицы, НомерСтроки, КонтейнерОтображаемойВерсии, КромеУказанной);
```
</details>

## УдалитьСтрокиТаблицыИзСтруктурыВерсии
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция УдалитьСтрокиТаблицыИзСтруктурыВерсии(СтруктураВерсииТитула, ИмяТаблицы, НомерСтроки = Неопределено, КромеУказанной = Ложь) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.УдалитьСтрокиТаблицыИзСтруктурыВерсии(СтруктураВерсииТитула, ИмяТаблицы, НомерСтроки, КромеУказанной);
```
</details>

## МассивЗначенийПоСтрокамТаблицыИзСтруктурыФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МассивЗначенийПоСтрокамТаблицыИзСтруктурыФормы(Форма, ИмяТаблицы, ИмяКолонки, КонтейнерОтображаемойВерсии = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.МассивЗначенийПоСтрокамТаблицыИзСтруктурыФормы(Форма, ИмяТаблицы, ИмяКолонки, КонтейнерОтображаемойВерсии);
```
</details>

## МассивСтрокТаблицыИзСтруктурыФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МассивСтрокТаблицыИзСтруктурыФормы(Форма, ИмяТаблицы, КонтейнерОтображаемойВерсии = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.МассивСтрокТаблицыИзСтруктурыФормы(Форма, ИмяТаблицы, КонтейнерОтображаемойВерсии);
```
</details>

## МассивСтрокТаблицыИзСтруктурыРеквизитов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МассивСтрокТаблицыИзСтруктурыРеквизитов(СтруктураРеквизитов, ИмяТаблицы) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.МассивСтрокТаблицыИзСтруктурыРеквизитов(СтруктураРеквизитов, ИмяТаблицы) 
```
</details>

## ОчиститьДанныеПоТитулуВСтруктуреФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОчиститьДанныеПоТитулуВСтруктуреФормы(Форма, Титул, КонтейнерОтображаемойВерсии = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ОчиститьДанныеПоТитулуВСтруктуреФормы(Форма, Титул, КонтейнерОтображаемойВерсии);
```
</details>

## ИзменитьОформлениеФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ИзменитьОформлениеФормы(Форма, ИмяЭлемента = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ИзменитьОформлениеФормы(Форма, ИмяЭлемента);
```
</details>

## РассчетНДСПоСтавкеСтрокой
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция РассчетНДСПоСтавкеСтрокой(СуммаБезНДС, СтавкаСтрокой) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.РассчетНДСПоСтавкеСтрокой(СуммаБезНДС, СтавкаСтрокой));
```
</details>

## ПолучитьПредставлениеТелефона
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПолучитьПредставлениеТелефона(Телефон) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьПредставлениеТелефона(Телефон) 
```
</details>

## ТитулПоПрефиксу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ТитулПоПрефиксу(ТипДокумента, ПрефиксТитула, ИндексТитула = 1) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ТитулПоПрефиксу(ТипДокумента, ПрефиксТитула, ИндексТитула);
```
</details>

## КонтейнерОтображаемойВерсииРеквизита
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция КонтейнерОтображаемойВерсииРеквизита(Форма, ИмяЭлемента, ИндексТитула = 1) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.КонтейнерОтображаемойВерсииРеквизита(Форма, ИмяЭлемента, ИндексТитула);
```
</details>

## ПолучитьКонтейнерРеквизитов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПолучитьКонтейнерРеквизитов(Форма, ИмяЭлемента = Неопределено, ТекущиеДанные = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьКонтейнерРеквизитов(Форма, ИмяЭлемента, ТекущиеДанные);
```
</details>

## ИзменитьОформлениеЭлементовФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ИзменитьОформлениеЭлементовФормы(Форма, ГруппаДанных = "") Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ИзменитьОформлениеЭлементовФормы(Форма, ГруппаДанных);
```
</details>

## ПолучитьСтруктуруАдресаУчастника
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПолучитьСтруктуруАдресаУчастника(ИмяРеквизита, Форма, ТекущиеДанные = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьСтруктуруАдресаУчастника(ИмяРеквизита, Форма, ТекущиеДанные);
```
</details>

## ЗаполнитьПредставлениеАдреса
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнитьПредставлениеАдреса(ИмяРеквизита, Форма, ТекущиеДанные = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ЗаполнитьПредставлениеАдреса(ИмяРеквизита, Форма, ТекущиеДанные);
```
</details>

## ПредставлениеКонтактнойИнформации
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПредставлениеКонтактнойИнформации(КонтактнаяИнформация) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПредставлениеКонтактнойИнформации(КонтактнаяИнформация) 
```
</details>

## РазделитьСтрокуСоСложнымРазделителем
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция РазделитьСтрокуСоСложнымРазделителем(Строка, Разделитель) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.РазделитьСтрокуСоСложнымРазделителем(Строка, Разделитель) 
```
</details>

## ЗаполнитьРеквизитыУчастника
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнитьРеквизитыУчастника(Участник, ПрефиксРеквизитов, КонтейнерРеквизитов, Форма, ПрефиксТитула = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ЗаполнитьРеквизитыУчастника(Участник, ПрефиксРеквизитов, КонтейнерРеквизитов, Форма, ПрефиксТитула);
```
</details>

## ЗаполнитьРеквизитыФизическогоЛица
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ЗаполнитьРеквизитыФизическогоЛица(ФизическоеЛицо, ПрефиксРеквизитов, КонтейнерРеквизитов, Организация = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ЗаполнитьРеквизитыФизическогоЛица(ФизическоеЛицо, ПрефиксРеквизитов, КонтейнерРеквизитов, Организация);
```
</details>

## ЗаполнитьРеквизитыВалюты
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ЗаполнитьРеквизитыВалюты(Валюта, ПрефиксРеквизитов, КонтейнерРеквизитов) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ЗаполнитьРеквизитыВалюты(Валюта, ПрефиксРеквизитов, КонтейнерРеквизитов) 
```
</details>

## ЗаполнитьРеквизитыКлассификатораДОПОГ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ЗаполнитьРеквизитыКлассификатораДОПОГ(ЗначениеКлассификатора, ПрефиксРеквизитов, КонтейнерРеквизитов) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ЗаполнитьРеквизитыКлассификатораДОПОГ(ЗначениеКлассификатора, ПрефиксРеквизитов, КонтейнерРеквизитов) 
```
</details>

## ЗаполнитьРеквизитыБанковскогоСчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ЗаполнитьРеквизитыБанковскогоСчета(БанковскийСчет, ПрефиксРеквизитов, КонтейнерРеквизитов) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ЗаполнитьРеквизитыБанковскогоСчета(БанковскийСчет, ПрефиксРеквизитов, КонтейнерРеквизитов) 
```
</details>

## ЗаполнитьРеквизитыАдреса
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ЗаполнитьРеквизитыАдреса(Форма, Результат, ИмяЭлемента, ТекущиеДанные = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ЗаполнитьРеквизитыАдреса(Форма, Результат, ИмяЭлемента, ТекущиеДанные);
```
</details>

## ОчиститьАдрес
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ОчиститьАдрес(КонтейнерРеквизитов, Префикс) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ОчиститьАдрес(КонтейнерРеквизитов, Префикс));
```
</details>

## ПолучитьТипОрганизация
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПолучитьТипОрганизация() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьТипОрганизация() 
```
</details>

## ПолучитьТипКонтрагент
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПолучитьТипКонтрагент() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьТипКонтрагент() 
```
</details>

## ПолучитьКонтрагентовДляФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьКонтрагентовДляФормы(КонтейнерРеквизитов, Форма) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьКонтрагентовДляФормы(КонтейнерРеквизитов, Форма) 
```
</details>

## ЗаполнитьРеквизитыПоСсылке
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнитьРеквизитыПоСсылке(Элемент, Форма, ТекущиеДанные = Неопределено, ПрефиксТитула = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ЗаполнитьРеквизитыПоСсылке(Элемент, Форма, ТекущиеДанные, ПрефиксТитула);
```
</details>

## ОкруглениеДатыПриИзменении
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ОкруглениеДатыПриИзменении(Знач Дата) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ОкруглениеДатыПриИзменении(Дата) 
```
</details>

## ОкруглениеДатыПриРегулировании
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОкруглениеДатыПриРегулировании(Дата, Направление, СтандартнаяОбработка) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ОкруглениеДатыПриРегулировании(Дата, Направление, СтандартнаяОбработка) 
```
</details>

## ОкруглитьВремя
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ОкруглитьВремя(ДатаИВремя, ШагОкругления = 0, ТолькоПрошедшеееВремя = Истина) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ОкруглитьВремя(ДатаИВремя, ШагОкругления, ТолькоПрошедшеееВремя);
```
</details>

## ШагОкругленияВремени
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ШагОкругленияВремени() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ШагОкругленияВремени());
```
</details>

## ГруппыХранимыхДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ГруппыХранимыхДанных(Знач ИмяОбъекта) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ГруппыХранимыхДанных(ИмяОбъекта) 
```
</details>

## ОписаниеГруппыДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ОписаниеГруппыДанных(Знач ГруппаДанных, Знач ИмяОбъекта) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ОписаниеГруппыДанных(ГруппаДанных, ИмяОбъекта) 
```
</details>

## ПостфиксДляИнформацииОбОшибке
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает постфикс для информации об ошибке.
//
// Возвращаемое значение:
//  Строка - Постфикс для информации об ошибке
Функция ПостфиксДляИнформацииОбОшибке() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПостфиксДляИнформацииОбОшибке() 
```
</details>

## ПоказатьОшибкиПоКонтрагентам
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Показывает ошибки по контрагентам.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
//  МассивЭлементовКонтрагентов - Массив - Массив элементов формы для проверки
Процедура ПоказатьОшибкиПоКонтрагентам(Форма, МассивЭлементовКонтрагентов) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ПоказатьОшибкиПоКонтрагентам(Форма, МассивЭлементовКонтрагентов) 
```
</details>

## ПоказатьОшибкиПоКонтрагенту
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПоказатьОшибкиПоКонтрагенту(Форма, ЭлементФормы, Контрагент) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ПоказатьОшибкиПоКонтрагенту(Форма, ЭлементФормы, Контрагент));
```
</details>

## ПоказатьОшибкиПоАдресу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПоказатьОшибкиПоАдресу(Форма, КонтейнерРеквизитов, Префикс, ИмяЭлемента) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ПоказатьОшибкиПоАдресу(Форма, КонтейнерРеквизитов, Префикс, ИмяЭлемента));
```
</details>

## НовоеОписаниеОшибокЗаполнения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НовоеОписаниеОшибокЗаполнения() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.НовоеОписаниеОшибокЗаполнения());
```
</details>

## ПолучитьОшибкиПоКонтрагенту
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОшибкиПоКонтрагенту(Форма, ЭлементФормы, Контрагент) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьОшибкиПоКонтрагенту(Форма, ЭлементФормы, Контрагент));
```
</details>

## ОформлениеКнопокНаФорме
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Оформляет кнопки на форме по переданому массиву оформления
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - Форма, в которой необходимо оформить кнопки
//	СтруктураРеквизитов - Структура - Структура содержащие данные титула
//  МассивОформленияКнопок - Массив - массив содержащий структуры см. ОбменСГИСЭПД.НоваяСтруктураОформленияКнопки()
//
Процедура ОформлениеКнопокНаФорме(Форма, СтруктураРеквизитов, МассивОформленияКнопок) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ОформлениеКнопокНаФорме(Форма, СтруктураРеквизитов, МассивОформленияКнопок) 
```
</details>

## ОформитьКнопкуСтрокиТабЧасти
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОформитьКнопкуСтрокиТабЧасти(Форма, Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ОформитьКнопкуСтрокиТабЧасти(Форма, );
```
</details>

## ОформитьКнопкуФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОформитьКнопкуФормы(Форма, Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ОформитьКнопкуФормы(Форма, );
```
</details>

## ОформлениеЭлементаНеобходимогоКЗаполнению
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОформлениеЭлементаНеобходимогоКЗаполнению(Форма, ПутьКЭлементуФормы) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ОформлениеЭлементаНеобходимогоКЗаполнению(Форма, ПутьКЭлементуФормы));
```
</details>

## ОформлениеЭлементаЗаполненногоБезОшибки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОформлениеЭлементаЗаполненногоБезОшибки(Форма, ПутьКЭлементуФормы, Заголовок = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ОформлениеЭлементаЗаполненногоБезОшибки(Форма, ПутьКЭлементуФормы, Заголовок);
```
</details>

## ОформлениеЭлементаНеОбязательно
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОформлениеЭлементаНеОбязательно(Форма, ПутьКЭлементуФормы) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиентСервер.ОформлениеЭлементаНеОбязательно(Форма, ПутьКЭлементуФормы));
```
</details>

## ТипДокументаПоИмениФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает тип документа по имени формы.
//
// Параметры:
//  ИмяФормы Имя формы
//
// Возвращаемое значение:
//  Строка - Тип документа по имени формы
Функция ТипДокументаПоИмениФормы(ИмяФормы) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ТипДокументаПоИмениФормы(ИмяФормы) 
```
</details>

## ПолучитьИндексПоТекущемуТитулу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьИндексПоТекущемуТитулу(ТекущийТитул) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьИндексПоТекущемуТитулу(ТекущийТитул));
```
</details>

## ПолучитьСтруктуруПоТитулуИВерсии
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает структуру реквизитов по титулу и версии.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
//
// Возвращаемое значение:
//  Неопределено, Структура - Структура для основной формы, Неопределено для подчиненных
Функция ПолучитьСтруктуруПоТитулуИВерсии(Форма) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьСтруктуруПоТитулуИВерсии(Форма) 
```
</details>

## ЭтоОсновнаяФорма
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Это основная форма.
//
// Параметры:
//  ПараметрЗадающийФорму - ФормаКлиентскогоПриложения, Строка - Параметр задающий форму
//
// Возвращаемое значение:
//  Булево - Истина если это основная форма
Функция ЭтоОсновнаяФорма(ПараметрЗадающийФорму) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ЭтоОсновнаяФорма(ПараметрЗадающийФорму) 
```
</details>

## ПолучитьПрефиксТитулаПоСтраницеОсновнойФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает префикс титула по странице основной формы.
//
// Параметры:
//  ИмяТекущейСтраницы - Строка - Имя текущей страницы
//  ТипДокумента - Строка - Тип документа
//
// Возвращаемое значение:
//  Строка - Получить префикс
Функция ПолучитьПрефиксТитулаПоСтраницеОсновнойФормы(ИмяТекущейСтраницы, ТипДокумента) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьПрефиксТитулаПоСтраницеОсновнойФормы(ИмяТекущейСтраницы, ТипДокумента) 
```
</details>

## ТипДокументаТранспортнаяНакладная
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает текстовое представление типа документа транспортной накладной.
//
// Возвращаемое значение:
//  Строка - текстовое представление типа документа транспортной накладной.
Функция ТипДокументаТранспортнаяНакладная() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ТипДокументаТранспортнаяНакладная() 
```
</details>

## ТипДокументаЗаказНаряд
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает текстовое представление типа документа заказ наряда.
//
// Возвращаемое значение:
//  Строка - текстовое представление типа документа заказ наряда.
Функция ТипДокументаЗаказНаряд() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ТипДокументаЗаказНаряд() 
```
</details>

## ТипДокументаЗаказЗаявка
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает текстовое представление типа документа заказа (заявки).
//
// Возвращаемое значение:
//  Строка - текстовое представление типа документа заказ наряда.
Функция ТипДокументаЗаказЗаявка() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ТипДокументаЗаказЗаявка() 
```
</details>

## ТипДокументаПутевойЛист
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает текстовое представление типа документа электронный путевой лист.
//
// Возвращаемое значение:
//  Строка - текстовое представление типа документа заказ наряда.
Функция ТипДокументаПутевойЛист() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ТипДокументаПутевойЛист() 
```
</details>

## ТипДокументаДоговорФрахтования
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает текстовое представление типа документа электронный путевой лист.
//
// Возвращаемое значение:
//  Строка - текстовое представление типа документа заказ наряда.
Функция ТипДокументаДоговорФрахтования() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ТипДокументаДоговорФрахтования() 
```
</details>

## ТипДокументаСопроводительнаяВедомость
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


// Возвращает текстовое представление типа документа сопроводительная ведомость.
//
// Возвращаемое значение:
//  Строка - текстовое представление типа документа сопроводительная ведомость.
Функция ТипДокументаСопроводительнаяВедомость() Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ТипДокументаСопроводительнаяВедомость() 
```
</details>

## ПолучитьСериализуемыйОбъектСДаннымиДокумента
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает сериализуемый объект содержащий данные документа.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения -
//
// Возвращаемое значение:
//  Структура - Структура с данными документа
Функция ПолучитьСериализуемыйОбъектСДаннымиДокумента(Форма) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.ПолучитьСериализуемыйОбъектСДаннымиДокумента(Форма) 
```
</details>

## СтруктураСДаннымиФормыДляОформленияКнопок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает структуру с данными формы, необходиммыми для оформления кнопок.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
//
// Возвращаемое значение:
//  Структура - Структура с данными формы для оформления кнопок:
// * ИмяФормы - Строка - Имя формы
// * АдресДереваСоответствийИтаблицыКнопок - Строка - Адрес дерева
// * УникальныйИдентификатор - Строка - Уникальный идентификатор формы
Функция СтруктураСДаннымиФормыДляОформленияКнопок(Форма) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиентСервер.СтруктураСДаннымиФормыДляОформленияКнопок(Форма) 
```
</details>

---

© 2024, **ООО 1С-Софт**  
Все права защищены. Эта программа и сопроводительные материалы предоставляются  
в соответствии с условиями лицензии [Attribution 4.0 International (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/legalcode).

---
