---
title: Пересчет показателей УХ
---


## ПолучитьСписокВерсий
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСписокВерсий(ОбъектРасчета,СтруктураКлючевыхРеквизитов,ТекВидОтчета,ПериодРасчета) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьСписокВерсий(ОбъектРасчета, СтруктураКлючевыхРеквизитов, ТекВидОтчета, ПериодРасчета));
```
</details>

## ИнициализироватьКонтекст
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ИнициализироватьКонтекст(ОбъектРасчета,СтруктураКлючевыхРеквизитов,ТекВидОтчета,ПериодРасчета,ОтборПоказателей) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ИнициализироватьКонтекст(ОбъектРасчета, СтруктураКлючевыхРеквизитов, ТекВидОтчета, ПериодРасчета, ОтборПоказателей) 
```
</details>

## ПодготовитьДанныеДляРасчетов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПодготовитьДанныеДляРасчетов(ОбъектРасчета,СтруктураКлючевыхРеквизитов,ТекВидОтчета,ПериодРасчета) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ПодготовитьДанныеДляРасчетов(ОбъектРасчета, СтруктураКлючевыхРеквизитов, ТекВидОтчета, ПериодРасчета));
```
</details>

## ИнициализироватьЛоги
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ИнициализироватьЛоги(ОбъектРасчета) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ИнициализироватьЛоги(ОбъектРасчета));
```
</details>

## ЕстьДанныеРасчетовПоказателя
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЕстьДанныеРасчетовПоказателя(ОбъектРасчета,НазначениеРасчетов,ПотребительРасчета) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ЕстьДанныеРасчетовПоказателя(ОбъектРасчета, НазначениеРасчетов, ПотребительРасчета));
```
</details>

## РассчитатьЗначениеПоказателя
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура РассчитатьЗначениеПоказателя(ОбъектРасчета, Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.РассчитатьЗначениеПоказателя(ОбъектРасчета, );
```
</details>

## ПроверитьИспользованиеПравила
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПроверитьИспользованиеПравила(ОбъектРасчета,ВидОтчетаПроверка,НазначениеРасчетовПроверка,ОрганизацияПроверка,СценарийПроверка,ПериодОтчетаПроверка) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПроверитьИспользованиеПравила(ОбъектРасчета, ВидОтчетаПроверка, НазначениеРасчетовПроверка, ОрганизацияПроверка, СценарийПроверка, ПериодОтчетаПроверка));
```
</details>

## ПолучитьСтруктуруРеквизитов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСтруктуруРеквизитов(ОбъектРасчета,ВидОтчетаПроверка,ОрганизацияПроверка,СценарийПроверка,ПериодОтчетаПроверка,НазначениеРасчетовПроверка=Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьСтруктуруРеквизитов(ОбъектРасчета, ВидОтчетаПроверка, ОрганизацияПроверка, СценарийПроверка, ПериодОтчетаПроверка, НазначениеРасчетовПроверка);
```
</details>

## ПолучитьВИБПоУмолчанию
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьВИБПоУмолчанию(ПравилоОбработки) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьВИБПоУмолчанию(ПравилоОбработки) 
```
</details>

## РассчитатьЗначенияПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура РассчитатьЗначенияПоказателей(ОбъектРасчета,ДанныеДляПересчетаПоказателей) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.РассчитатьЗначенияПоказателей(ОбъектРасчета, ДанныеДляПересчетаПоказателей) 
```
</details>

## ЗаписатьЗначенияПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаписатьЗначенияПоказателей(ОбъектРасчета,СтруктураКлючевыхРеквизитов,ЗапросДанные) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ЗаписатьЗначенияПоказателей(ОбъектРасчета, СтруктураКлючевыхРеквизитов, ЗапросДанные) 
```
</details>

## РассчитатьСтрокуВалютногоПоказателя
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура РассчитатьСтрокуВалютногоПоказателя(ОбъектРасчета,СтруктураКлючевыхРеквизитов,СтрокаДанные,Показатель) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.РассчитатьСтрокуВалютногоПоказателя(ОбъектРасчета, СтруктураКлючевыхРеквизитов, СтрокаДанные, Показатель));
```
</details>

## ПолучитьДанныеКурса
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьДанныеКурса(ОбъектРасчета,Валюта,Сценарий,ПериодКурса,ВидКурса) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьДанныеКурса(ОбъектРасчета, Валюта, Сценарий, ПериодКурса, ВидКурса));
```
</details>

## ПолучитьМассивОткрытыхПериодовДляПересчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьМассивОткрытыхПериодовДляПересчета(ПериодРасчета,Организация,Сценарий) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьМассивОткрытыхПериодовДляПересчета(ПериодРасчета, Организация, Сценарий));
```
</details>

## РассчитатьРекурсивноЗависимыеПоказатели
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура РассчитатьРекурсивноЗависимыеПоказатели(ДанныеПоказателей,ОбъектРасчета,СтруктураКлючевыхРеквизитов,ПересчитыватьТекущие=Истина) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.РассчитатьРекурсивноЗависимыеПоказатели(ДанныеПоказателей, ОбъектРасчета, СтруктураКлючевыхРеквизитов, ПересчитыватьТекущие);
```
</details>

## ДобавитьДанныеПересчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьДанныеПересчета(ИдентификаторПересчета,СтруктураКлючевыхРеквизитов,Показатель,ТаблицаИсходныхПоказателей,ТаблицаТрассировкиРасчета,ТаблицаРасчетаПоказателей,мПоказатели) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ДобавитьДанныеПересчета(ИдентификаторПересчета, СтруктураКлючевыхРеквизитов, Показатель, ТаблицаИсходныхПоказателей, ТаблицаТрассировкиРасчета, ТаблицаРасчетаПоказателей, мПоказатели));
```
</details>

## ВыполнятьРасчетПриАктуализации
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВыполнятьРасчетПриАктуализации(ОбъектРасчета,СтрПоказатель,ПериодРасчета) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ВыполнятьРасчетПриАктуализации(ОбъектРасчета, СтрПоказатель, ПериодРасчета));
```
</details>

## РассчитатьРекурсивноЗависимыеПоказателиПоТаблице
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РассчитатьРекурсивноЗависимыеПоказателиПоТаблице(ТаблицаЗависимыхПоказателей,ОбъектРасчета,СтруктураКлючевыхРеквизитов,ПересчитыватьТекущие,ТрассировкаРасчета) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.РассчитатьРекурсивноЗависимыеПоказателиПоТаблице(ТаблицаЗависимыхПоказателей, ОбъектРасчета, СтруктураКлючевыхРеквизитов, ПересчитыватьТекущие, ТрассировкаРасчета));
```
</details>

## ПолучитьТаблицуЗависимыхПоМассиву
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТаблицуЗависимыхПоМассиву(ОбъектРасчета,МассивПоказатели,СтруктураКлючевыхРеквизитов) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьТаблицуЗависимыхПоМассиву(ОбъектРасчета, МассивПоказатели, СтруктураКлючевыхРеквизитов));
```
</details>

## ПолучитьТаблицуЗависимыхПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТаблицуЗависимыхПоказателей(ОбъектРасчета,ПотребительРасчета,СтруктураКлючевыхРеквизитов) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьТаблицуЗависимыхПоказателей(ОбъектРасчета, ПотребительРасчета, СтруктураКлючевыхРеквизитов));
```
</details>

## ЗаписатьИзмененияВРегистр
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗаписатьИзмененияВРегистр(ОбъектРасчета) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ЗаписатьИзмененияВРегистр(ОбъектРасчета));
```
</details>

## ИнициализироватьСоответствияДляПоиска
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ИнициализироватьСоответствияДляПоиска(ОбъектРасчета) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ИнициализироватьСоответствияДляПоиска(ОбъектРасчета) 
```
</details>

## ПолучитьСтруктуруПараметровДляРасшифровки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСтруктуруПараметровДляРасшифровки(ОбъектРасчета) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьСтруктуруПараметровДляРасшифровки(ОбъектРасчета) 
```
</details>

## ДобавитьСтрокиДереваПересчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьСтрокиДереваПересчета(ОбъектРасчета,СтрокаРодитель,СтруктураКлючевыхРеквизитов,ТекВидОтчета,ТекПотребительРасчета,ТекНазначениеРасчетов,ПериодРасчета) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ДобавитьСтрокиДереваПересчета(ОбъектРасчета, СтрокаРодитель, СтруктураКлючевыхРеквизитов, ТекВидОтчета, ТекПотребительРасчета, ТекНазначениеРасчетов, ПериодРасчета));
```
</details>

## ДобавитьСтрокиПересчетаРекурсивно
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьСтрокиПересчетаРекурсивно(ОбъектРасчета,СтрокаРодитель,МаксЧислоПроходов) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ДобавитьСтрокиПересчетаРекурсивно(ОбъектРасчета, СтрокаРодитель, МаксЧислоПроходов));
```
</details>

## ПериодЗакрытДляРасчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПериодЗакрытДляРасчета(ОбъектРасчета,ТекущийПериод) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПериодЗакрытДляРасчета(ОбъектРасчета, ТекущийПериод));
```
</details>

## ПолучитьОтносительныйПериод
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОтносительныйПериод(ОбъектРасчета,ПериодОтчета,Смещение) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьОтносительныйПериод(ОбъектРасчета, ПериодОтчета, Смещение));
```
</details>

## УвеличитьСчетчикПереходовРекурсивно
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УвеличитьСчетчикПереходовРекурсивно(ОбъектРасчета,СтрокаПересчета) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.УвеличитьСчетчикПереходовРекурсивно(ОбъектРасчета, СтрокаПересчета));
```
</details>

## УдалитьОписанияЧерновыхВерсий
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УдалитьОписанияЧерновыхВерсий(МассивОписаний) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.УдалитьОписанияЧерновыхВерсий(МассивОписаний) 
```
</details>

## ПолучитьОписаниеКонтекста
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОписаниеКонтекста(ОбъектРасчета) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьОписаниеКонтекста(ОбъектРасчета) 
```
</details>

## ВыполнитьПростуюЭлиминацию
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВыполнитьПростуюЭлиминацию(ОбъектРасчета,ДляРасшифровки=Ложь) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ВыполнитьПростуюЭлиминацию(ОбъектРасчета, ДляРасшифровки);
```
</details>

## ДобавитьЗначенияВТаблицуРасшифровки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьЗначенияВТаблицуРасшифровки(ОбъектРасчета,ЗапросРаскрытие) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ДобавитьЗначенияВТаблицуРасшифровки(ОбъектРасчета, ЗапросРаскрытие));
```
</details>

## ЗаписатьПоказателиОтчетовВОбъект
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаписатьПоказателиОтчетовВОбъект(ОбъектРасчета,Запрос,Показатель) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ЗаписатьПоказателиОтчетовВОбъект(ОбъектРасчета, Запрос, Показатель));
```
</details>

## ДополнитьВнутригрупповымиКонтрагентами
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДополнитьВнутригрупповымиКонтрагентами(МассивОрганизацийПериметра) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ДополнитьВнутригрупповымиКонтрагентами(МассивОрганизацийПериметра));
```
</details>

## ПолучитьВерсииКонсолидируюмыхОтчетов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Функция - Получить версии консолидируюмых отчетов
//
// Параметры:
//  СписокПериметра	 - массив организаций для отбора версий
// Возвращаемое значение:
//   - массив версий значений показателей
Функция ПолучитьВерсииКонсолидируюмыхОтчетов(ОбъектРасчета,СписокПериметра) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьВерсииКонсолидируюмыхОтчетов(ОбъектРасчета, СписокПериметра) 
```
</details>

## ПолучитьТаблицуРаскрытияПоПараметрам
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

//
Функция ПолучитьТаблицуРаскрытияПоПараметрам(Организация, Сценарий, ВидОтчета, ПериодОтчета, Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьТаблицуРаскрытияПоПараметрам(Организация, Сценарий, ВидОтчета, ПериодОтчета, );
```
</details>

## ПолучитьПустуюТаблицуРаскрытий
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПустуюТаблицуРаскрытий(ОбъектРасчета) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьПустуюТаблицуРаскрытий(ОбъектРасчета) 
```
</details>

## ПолучитьОбъектРасчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОбъектРасчета(Ссылка) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьОбъектРасчета(Ссылка) 
```
</details>

## ДополнитьТекстДополнительныеОтборы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДополнитьТекстДополнительныеОтборы(Запрос,ДополнительныеОтборы) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ДополнитьТекстДополнительныеОтборы(Запрос, ДополнительныеОтборы));
```
</details>

## СвернутьВерсииПоРеквизитам
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура СвернутьВерсииПоРеквизитам(УправлениеПериодом=Неопределено, Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.СвернутьВерсииПоРеквизитам(УправлениеПериодом, );
```
</details>

## СвернутьВерсии
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СвернутьВерсии(ТаблицаВерсийКСвертке,ОбъектРасчета,ПоследнееОбнаруженноеОписание,Отказ) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.СвернутьВерсии(ТаблицаВерсийКСвертке, ОбъектРасчета, ПоследнееОбнаруженноеОписание, Отказ));
```
</details>

## ДобавитьЗаписиВНаборыЧисловые
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьЗаписиВНаборыЧисловые(Версия, Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ДобавитьЗаписиВНаборыЧисловые(Версия, );
```
</details>

## ДобавитьЗаписиВНаборыНеЧисловые
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьЗаписиВНаборыНеЧисловые(Версия, Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ДобавитьЗаписиВНаборыНеЧисловые(Версия, );
```
</details>

## ДобавитьЗаписьВНабор
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ДобавитьЗаписьВНабор(Версия, Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ДобавитьЗаписьВНабор(Версия, );
```
</details>

## ЗаполнитьКолонкуВерсий
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнитьКолонкуВерсий(ОбъектРасчета,РабочаяТаблица,ТаблицаВерсий,ОписаниеВерсии) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ЗаполнитьКолонкуВерсий(ОбъектРасчета, РабочаяТаблица, ТаблицаВерсий, ОписаниеВерсии));
```
</details>

## ПолучитьТаблицуКомментариев
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТаблицуКомментариев(СписокВерсий) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьТаблицуКомментариев(СписокВерсий));
```
</details>

## ПолучитьТаблицуНечисловых
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТаблицуНечисловых(СписокВерсий) Экспорт
```

Пример вызова
```bsl
Результат = ПересчетПоказателейУХ.ПолучитьТаблицуНечисловых(СписокВерсий));
```
</details>

## ЗаписатьТаблицуКомментариев
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаписатьТаблицуКомментариев(Версия,ТаблицаКомментариев) Экспорт
```

Пример вызова
```bsl
ПересчетПоказателейУХ.ЗаписатьТаблицуКомментариев(Версия, ТаблицаКомментариев));
```
</details>

---

© 2024, **ООО 1С-Софт**  
Все права защищены. Эта программа и сопроводительные материалы предоставляются  
в соответствии с условиями лицензии [Attribution 4.0 International (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/legalcode).

---
