---
title: Структура заказа
---


## ВыполнитьРаспределениеЗапасов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВыполнитьРаспределениеЗапасов(ПараметрыДействия, АдресРезультата) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ВыполнитьРаспределениеЗапасов(ПараметрыДействия, АдресРезультата) 
```
</details>

## ВыполнитьРаспределениеЗапасовВнутриЗамераВремени
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВыполнитьРаспределениеЗапасовВнутриЗамераВремени(ПараметрыДействия) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ВыполнитьРаспределениеЗапасовВнутриЗамераВремени(ПараметрыДействия));
```
</details>

## РаспределитьЗапасы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура РаспределитьЗапасы(РезультатТребуется, РезультатЗапасы, РезультатРаспределения) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.РаспределитьЗапасы(РезультатТребуется, РезультатЗапасы, РезультатРаспределения));
```
</details>

## ГрафикТовара
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ГрафикТовара(График, Аналитика, Граница, Индекс, УсловияРасчета, ИзменитьАналитику = Ложь) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ГрафикТовара(График, Аналитика, Граница, Индекс, УсловияРасчета, ИзменитьАналитику);
```
</details>

## СледующийГрафикТовара
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СледующийГрафикТовара(ГрафикТовара, Аналитика, Граница, Индекс, УсловияРасчета) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.СледующийГрафикТовара(ГрафикТовара, Аналитика, Граница, Индекс, УсловияРасчета));
```
</details>

## ЗапасДоступен
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗапасДоступен(Выборка, Запас) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ЗапасДоступен(Выборка, Запас));
```
</details>

## ПоместитьРезультатРаспределенияЗапасовВМенеджерВременныхТаблиц
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПоместитьРезультатРаспределенияЗапасовВМенеджерВременныхТаблиц(МенеджерВременныхТаблиц, РезультатРаспределения) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ПоместитьРезультатРаспределенияЗапасовВМенеджерВременныхТаблиц(МенеджерВременныхТаблиц, РезультатРаспределения));
```
</details>

## РассчитатьИзмененияИЗаписатьРезультатРаспределенияЗапасов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура РассчитатьИзмененияИЗаписатьРезультатРаспределенияЗапасов(МенеджерВременныхТаблиц, ПараметрыРасчета) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.РассчитатьИзмененияИЗаписатьРезультатРаспределенияЗапасов(МенеджерВременныхТаблиц, ПараметрыРасчета));
```
</details>

## ПолучитьИПоместитьВКешСпособыОбеспечения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьИПоместитьВКешСпособыОбеспечения(МенеджерВременныхТаблиц, ИмяВременнойТаблицы = "НоменклатурныйПлан") Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ПолучитьИПоместитьВКешСпособыОбеспечения(МенеджерВременныхТаблиц, ИмяВременнойТаблицы);
```
</details>

## ЗапланироватьПроизводство
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗапланироватьПроизводство(ПараметрыРасчета, МенеджерВременныхТаблиц, ИмяВременнойТаблицы = "ПланПроизводства") Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ЗапланироватьПроизводство(ПараметрыРасчета, МенеджерВременныхТаблиц, ИмяВременнойТаблицы);
```
</details>

## ЗапланироватьПеремещение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗапланироватьПеремещение(ПараметрыРасчета, МенеджерВременныхТаблиц, ИмяВременнойТаблицы = "ПланПотребления") Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ЗапланироватьПеремещение(ПараметрыРасчета, МенеджерВременныхТаблиц, ИмяВременнойТаблицы);
```
</details>

## УдалитьПроблемныеНаборыЗаписейПоАналитике
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УдалитьПроблемныеНаборыЗаписейПоАналитике(Аналитика, ВидСтроки, КлючНабор = Null) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.УдалитьПроблемныеНаборыЗаписейПоАналитике(Аналитика, ВидСтроки, КлючНабор);
```
</details>

## УдалитьНаборЗаписейПоКлючу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УдалитьНаборЗаписейПоКлючу(КлючНабор) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.УдалитьНаборЗаписейПоКлючу(КлючНабор) 
```
</details>

## ПараметрыВыборкиДанныхСпецификации
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПараметрыВыборкиДанныхСпецификации() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ПараметрыВыборкиДанныхСпецификации());
```
</details>

## ДанныеСпецификации
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ДанныеСпецификации(Изделие, КоличествоИзделия, ДанныеСпецификаций) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ДанныеСпецификации(Изделие, КоличествоИзделия, ДанныеСпецификаций));
```
</details>

## ВыполнитьПоток
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВыполнитьПоток(НаименованиеЗадания, ИмяПроцедуры, ПараметрыПроцедуры, ПротоколРасчета) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ВыполнитьПоток(НаименованиеЗадания, ИмяПроцедуры, ПараметрыПроцедуры, ПротоколРасчета));
```
</details>

## ЕстьСвободныеПотоки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


////////////////////////////////////////////////////////////////////////////////
// Работа с потоками в памяти.

Функция ЕстьСвободныеПотоки(ПротоколРасчета) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ЕстьСвободныеПотоки(ПротоколРасчета));
```
</details>

## ПолучитьМестоВОчередиРасчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьМестоВОчередиРасчета(ПротоколРасчета) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ПолучитьМестоВОчередиРасчета(ПротоколРасчета));
```
</details>

## ОжидатьЗавершениеРасчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ОжидатьЗавершениеРасчета(ПротоколРасчета, ОжидатьЗавершениеРасчетаВсехПотоков = Истина) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ОжидатьЗавершениеРасчета(ПротоколРасчета, ОжидатьЗавершениеРасчетаВсехПотоков);
```
</details>

## ЗаданиеВыполнено
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗаданиеВыполнено(ПротоколРасчета, ОписаниеПотока) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ЗаданиеВыполнено(ПротоколРасчета, ОписаниеПотока));
```
</details>

## СохранитьПоток
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

////////////////////////////////////////////////////////////////////////////////
// Работа с потоками в базе данных.

Процедура СохранитьПоток(ИдентификаторПотока) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.СохранитьПоток(ИдентификаторПотока));
```
</details>

## УдалитьВсеПотоки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УдалитьВсеПотоки() Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.УдалитьВсеПотоки());
```
</details>

## УдалитьПоток
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УдалитьПоток(ИдентификаторПотока) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.УдалитьПоток(ИдентификаторПотока));
```
</details>

## ОтменитьВыполнениеВсехПотоков
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОтменитьВыполнениеВсехПотоков() Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ОтменитьВыполнениеВсехПотоков());
```
</details>

## Потоки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция Потоки() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.Потоки());
```
</details>

## НайденыОшибкиПриРасчете
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


////////////////////////////////////////////////////////////////////////////////
// Работа с ошибками.

Функция НайденыОшибкиПриРасчете(ПротоколРасчета, ПорцияРасчета, ТекущееДействие) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.НайденыОшибкиПриРасчете(ПротоколРасчета, ПорцияРасчета, ТекущееДействие));
```
</details>

## ПроверитьСтатистикуОшибок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПроверитьСтатистикуОшибок(ПротоколРасчета) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ПроверитьСтатистикуОшибок(ПротоколРасчета));
```
</details>

## ЗаписатьРезультатВПротоколРасчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

////////////////////////////////////////////////////////////////////////////////
// Протокола расчета.

Процедура ЗаписатьРезультатВПротоколРасчета(ПротоколРасчета, АдресРезультата) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ЗаписатьРезультатВПротоколРасчета(ПротоколРасчета, АдресРезультата));
```
</details>

## ЗаписатьОшибкуВПротоколРасчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаписатьОшибкуВПротоколРасчета(ПротоколРасчета, ПараметрыПроцедуры, КраткоеПредставлениеОшибки = "", ПодробноеПредставлениеОшибки = "") Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ЗаписатьОшибкуВПротоколРасчета(ПротоколРасчета, ПараметрыПроцедуры, КраткоеПредставлениеОшибки, ПодробноеПредставлениеОшибки);
```
</details>

## СократитьПротокол
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СократитьПротокол(ПротоколРасчета) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.СократитьПротокол(ПротоколРасчета));
```
</details>

## ПараметрыРасчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПараметрыРасчета() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ПараметрыРасчета() 
```
</details>

## СлужебныеНастройки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СлужебныеНастройки(ПроверитьРежимЭксплуатации = Истина) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.СлужебныеНастройки(ПроверитьРежимЭксплуатации);
```
</details>

## СтандартныеНастройкиРасчетаСтруктурыЗаказа
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтандартныеНастройкиРасчетаСтруктурыЗаказа() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.СтандартныеНастройкиРасчетаСтруктурыЗаказа() 
```
</details>

## ЗаписатьНастройкиРасчетаСтруктурыЗаказа
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаписатьНастройкиРасчетаСтруктурыЗаказа(ЗначенияНастроек) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ЗаписатьНастройкиРасчетаСтруктурыЗаказа(ЗначенияНастроек) 
```
</details>

## ИнициализироватьНастройкиРасчетаСтруктурыЗаказа
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ИнициализироватьНастройкиРасчетаСтруктурыЗаказа() Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ИнициализироватьНастройкиРасчетаСтруктурыЗаказа() 
```
</details>

## УстановитьСтандартныеНастройкиРасчетаСтруктурыЗаказа
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьСтандартныеНастройкиРасчетаСтруктурыЗаказа() Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.УстановитьСтандартныеНастройкиРасчетаСтруктурыЗаказа() 
```
</details>

## УстановитьНастройкиРеглЗаданияСверткаСторноЗаписейВГрафикеПроизводства
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьНастройкиРеглЗаданияСверткаСторноЗаписейВГрафикеПроизводства(ПараметрыЗадания) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.УстановитьНастройкиРеглЗаданияСверткаСторноЗаписейВГрафикеПроизводства(ПараметрыЗадания));
```
</details>

## ИспользоватьРеглЗаданиеСверткаСторноЗаписейВГрафикеПроизводства
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИспользоватьРеглЗаданиеСверткаСторноЗаписейВГрафикеПроизводства() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ИспользоватьРеглЗаданиеСверткаСторноЗаписейВГрафикеПроизводства());
```
</details>

## УстановитьИспользованиеРеглЗаданияСверткаСторноЗаписейВГрафикеПроизводства
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьИспользованиеРеглЗаданияСверткаСторноЗаписейВГрафикеПроизводства() Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.УстановитьИспользованиеРеглЗаданияСверткаСторноЗаписейВГрафикеПроизводства() 
```
</details>

## УстановитьРасписаниеРеглЗаданияСверткаСторноЗаписейВГрафикеПроизводства
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УстановитьРасписаниеРеглЗаданияСверткаСторноЗаписейВГрафикеПроизводства(РасписаниеРегламентногоЗадания) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.УстановитьРасписаниеРеглЗаданияСверткаСторноЗаписейВГрафикеПроизводства(РасписаниеРегламентногоЗадания) 
```
</details>

## ИнтервалПроверкиЗавершенияРасчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИнтервалПроверкиЗавершенияРасчета() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ИнтервалПроверкиЗавершенияРасчета());
```
</details>

## КоличествоСтрокВНабореЗаписей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция КоличествоСтрокВНабореЗаписей() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.КоличествоСтрокВНабореЗаписей() 
```
</details>

## МаксимальноеКоличествоСтрокВНабореЗаписей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МаксимальноеКоличествоСтрокВНабореЗаписей() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.МаксимальноеКоличествоСтрокВНабореЗаписей());
```
</details>

## КоличествоПопытокЗаписи
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция КоличествоПопытокЗаписи() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.КоличествоПопытокЗаписи());
```
</details>

## ПротоколРасчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПротоколРасчета() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ПротоколРасчета());
```
</details>

## Статистика
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция Статистика() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.Статистика());
```
</details>

## ЗамерыКода
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗамерыКода() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ЗамерыКода() 
```
</details>

## НачатьЗамер
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НачатьЗамер(ИмяОперации, ПротоколРасчета) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.НачатьЗамер(ИмяОперации, ПротоколРасчета) 
```
</details>

## ЗавершитьЗамер
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗавершитьЗамер(ЗамерКода, КоличествоСтрок = 0) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ЗавершитьЗамер(ЗамерКода, КоличествоСтрок);
```
</details>

## РасшифровкаПротоколаРасчетаПоЗамеруКода
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РасшифровкаПротоколаРасчетаПоЗамеруКода(Таблица) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.РасшифровкаПротоколаРасчетаПоЗамеруКода(Таблица));
```
</details>

## ЗаписатьПротоколРасчетаВЖурналРегистрации
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаписатьПротоколРасчетаВЖурналРегистрации(ПротоколРасчета) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ЗаписатьПротоколРасчетаВЖурналРегистрации(ПротоколРасчета));
```
</details>

## ЗаписатьНеудачнуюПопыткуЗаписиВЖурналРегистрации
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаписатьНеудачнуюПопыткуЗаписиВЖурналРегистрации(СчетчикОшибок, ТекстОшибки) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ЗаписатьНеудачнуюПопыткуЗаписиВЖурналРегистрации(СчетчикОшибок, ТекстОшибки) 
```
</details>

## ЗаполнитьИЗаписатьНаборЗаписей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнитьИЗаписатьНаборЗаписей( Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ЗаполнитьИЗаписатьНаборЗаписей();
```
</details>

## ЗагрузитьСтрокиВНаборЗаписейСДопОбработкой
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗагрузитьСтрокиВНаборЗаписейСДопОбработкой(НаборЗаписей, Таблица, СписокСтрок = Неопределено, ЕстьРазделитель = Ложь, КолонкиПоЗначению = Неопределено) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ЗагрузитьСтрокиВНаборЗаписейСДопОбработкой(НаборЗаписей, Таблица, СписокСтрок, ЕстьРазделитель, КолонкиПоЗначению);
```
</details>

## ЗаписатьНаборЗаписейВПопытке
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаписатьНаборЗаписейВПопытке(НаборЗаписей, Замещать) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ЗаписатьНаборЗаписейВПопытке(НаборЗаписей, Замещать));
```
</details>

## РазбитьНаПорции
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Разбивает исходную таблицу на порции
//
// Параметры:
// 	Таблица - ТаблицаЗначений - исходная таблица
// 	ПоляВыборки - Строка - поля выборки
// 	ПоляГруппировки - Строка - поля группировки
// 	КоличествоПорций - Число - количество порций
// 	ТаблицаУпорядочена - Булево - призна, таблица упорядочена
// Возвращаемое значение:
// 	Массив - порции
//
Функция РазбитьНаПорции(Таблица, ПоляВыборки, ПоляГруппировки = "", КоличествоПорций = 1, ТаблицаУпорядочена = Истина) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.РазбитьНаПорции(Таблица, ПоляВыборки, ПоляГруппировки, КоличествоПорций, ТаблицаУпорядочена);
```
</details>

## СоединитьТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СоединитьТаблицы(Таблица, Таблица1) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.СоединитьТаблицы(Таблица, Таблица1));
```
</details>

## ЕстьРазличияВКолонках
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЕстьРазличияВКолонках(Строка1, Строка2, Колонки) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ЕстьРазличияВКолонках(Строка1, Строка2, Колонки));
```
</details>

## СравнитьЗначенияПолейКлюча
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СравнитьЗначенияПолейКлюча(ПервоеЗначение, ВтороеЗначение, ПоляКлюча, СравнениеЗначений) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.СравнитьЗначенияПолейКлюча(ПервоеЗначение, ВтороеЗначение, ПоляКлюча, СравнениеЗначений) 
```
</details>

## СкопироватьКолонки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СкопироватьКолонки(Колонки, КопируемыеКолонки = "", ИсключаемыеКолонки = "", ДобавляемыеКолонки = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.СкопироватьКолонки(Колонки, КопируемыеКолонки, ИсключаемыеКолонки, ДобавляемыеКолонки);
```
</details>

## НайтиСтрокуПоЗначениюКолонкиВМассивеСтрок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НайтиСтрокуПоЗначениюКолонкиВМассивеСтрок(МассивСтрок, ИмяКолонки, Значение) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.НайтиСтрокуПоЗначениюКолонкиВМассивеСтрок(МассивСтрок, ИмяКолонки, Значение));
```
</details>

## СвернутьТаблицуИУдалитьНулевыеСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СвернутьТаблицуИУдалитьНулевыеСтроки(Таблица, КолонкиГруппировки, КолонкиСуммирования) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.СвернутьТаблицуИУдалитьНулевыеСтроки(Таблица, КолонкиГруппировки, КолонкиСуммирования));
```
</details>

## УдалитьСтрокиИзТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура УдалитьСтрокиИзТаблицы(Таблица, Строки) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.УдалитьСтрокиИзТаблицы(Таблица, Строки));
```
</details>

## ПривестиЗначение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПривестиЗначение(ОписаниеТипов, Значение) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ПривестиЗначение(ОписаниеТипов, Значение));
```
</details>

## ИменаКолонокТаблицыСтрокой
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИменаКолонокТаблицыСтрокой(ИсточникКолонок, ИсключаемыеКолонки = "") Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ИменаКолонокТаблицыСтрокой(ИсточникКолонок, ИсключаемыеКолонки);
```
</details>

## ТекстЗапросаПоместитьТаблицуЗначенийВоВременнуюТаблицу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ТекстЗапросаПоместитьТаблицуЗначенийВоВременнуюТаблицу(ИмяВременнойТаблицы, Колонки, Знач ПоляТаблицы = "", Знач УсловиеОтбора = "", Знач ПоляИндексирования = "") Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ТекстЗапросаПоместитьТаблицуЗначенийВоВременнуюТаблицу(ИмяВременнойТаблицы, Колонки, ПоляТаблицы, УсловиеОтбора, ПоляИндексирования);
```
</details>

## ПоместитьТаблицуЗначенийВоВременнуюТаблицу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПоместитьТаблицуЗначенийВоВременнуюТаблицу(МенеджерВременныхТаблиц, ИмяВременнойТаблицы, Таблица, Знач ПоляТаблицы = "", Знач УсловиеОтбора = "", Знач ПоляИндексирования = "") Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.ПоместитьТаблицуЗначенийВоВременнуюТаблицу(МенеджерВременныхТаблиц, ИмяВременнойТаблицы, Таблица, ПоляТаблицы, УсловиеОтбора, ПоляИндексирования);
```
</details>

## РеквизитКоллекции
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция РеквизитКоллекции(Коллекция, ИмяРеквизита) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.РеквизитКоллекции(Коллекция, ИмяРеквизита));
```
</details>

## СтруктураПоПолям
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СтруктураПоПолям(Коллекция, СписокПолей) Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.СтруктураПоПолям(Коллекция, СписокПолей));
```
</details>

## ГраницаПериода
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ГраницаПериода() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ГраницаПериода());
```
</details>

## ТипЧисло
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ТипЧисло() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ТипЧисло());
```
</details>

## СортироватьМассивСтрок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура СортироватьМассивСтрок(Массив, Колонка, lb, ub) Экспорт
```

Пример вызова
```bsl
СтруктураЗаказа.СортироватьМассивСтрок(Массив, Колонка, lb, ub));
```
</details>

## ИмяСобытияЖурналаРегистрации
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИмяСобытияЖурналаРегистрации() Экспорт
```

Пример вызова
```bsl
Результат = СтруктураЗаказа.ИмяСобытияЖурналаРегистрации() 
```
</details>

---

© 2024, **ООО 1С-Софт**  
Все права защищены. Эта программа и сопроводительные материалы предоставляются  
в соответствии с условиями лицензии [Attribution 4.0 International (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/legalcode).

---
