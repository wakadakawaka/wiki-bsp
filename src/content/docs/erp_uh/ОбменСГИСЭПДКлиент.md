---
title: Обмен с ГИС ЭПД клиент
slug: erp-uh/обмен-с-гис-эпд-клиент
---


## БаннерПриНажатии
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура БаннерПриНажатии(Элемент, ДанныеСобытия, СтандартнаяОбработка) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.БаннерПриНажатии(Элемент, ДанныеСобытия, СтандартнаяОбработка) 
```
</details>

## БаннерКомандаЗакрыть
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура БаннерКомандаЗакрыть(Элемент) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.БаннерКомандаЗакрыть(Элемент) 
```
</details>

## БаннерПриНажатииПослеВопроса
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура БаннерПриНажатииПослеВопроса(РезультатВопроса, ДополнительныеПараметры) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.БаннерПриНажатииПослеВопроса(РезультатВопроса, ДополнительныеПараметры) 
```
</details>

## ПоказатьХранимыеДанныеЭПД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПоказатьХранимыеДанныеЭПД(ГруппаДанных, Отбор, ФормаВладелец, ОписаниеОповещения, Отказ, ИдентификаторСтрокиРодителя = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ПоказатьХранимыеДанныеЭПД(ГруппаДанных, Отбор, ФормаВладелец, ОписаниеОповещения, Отказ, ИдентификаторСтрокиРодителя);
```
</details>

## ПриИзмененииДанныхНаФорме
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПриИзмененииДанныхНаФорме(Форма, ИмяЭлемента) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ПриИзмененииДанныхНаФорме(Форма, ИмяЭлемента) 
```
</details>

## ХранимыеДанныеНачалоВыбора
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ХранимыеДанныеНачалоВыбора(Форма, Элемент, ДанныеВыбора, СтандартнаяОбработка, ТекущиеДанные = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ХранимыеДанныеНачалоВыбора(Форма, Элемент, ДанныеВыбора, СтандартнаяОбработка, ТекущиеДанные);
```
</details>

## ХранимыеДанныеПриИзменении
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ХранимыеДанныеПриИзменении(Форма, ЗначениеРеквизита, Элемент, ТекущиеДанные = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ХранимыеДанныеПриИзменении(Форма, ЗначениеРеквизита, Элемент, ТекущиеДанные);
```
</details>

## ХранимыеДанныеАвтоПодбор
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ХранимыеДанныеАвтоПодбор(Форма, Элемент, Текст, ДанныеВыбора, ПараметрыПолученияДанных, Ожидание, СтандартнаяОбработка) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ХранимыеДанныеАвтоПодбор(Форма, Элемент, Текст, ДанныеВыбора, ПараметрыПолученияДанных, Ожидание, СтандартнаяОбработка) 
```
</details>

## ХранимыеДанныеОткрытие
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ХранимыеДанныеОткрытие(Форма, ЗначениеРеквизита, Элемент, СтандартнаяОбработка) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ХранимыеДанныеОткрытие(Форма, ЗначениеРеквизита, Элемент, СтандартнаяОбработка) 
```
</details>

## ТекстВопросаОПоддержкеОператоромЭПД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ТекстВопросаОПоддержкеОператоромЭПД(ТипДокумента, ПриСозданииДокумента = Ложь) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиент.ТекстВопросаОПоддержкеОператоромЭПД(ТипДокумента, ПриСозданииДокумента);
```
</details>

## ВопросОФормированииЭПД_Завершение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ВопросОФормированииЭПД_Завершение(Результат, ТекущиеДанные) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ВопросОФормированииЭПД_Завершение(Результат, ТекущиеДанные) 
```
</details>

## ПолучитьНастройкиОтправкиЭДО
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьНастройкиОтправкиЭДО(Организация, Контрагент, ВидДокумента, ОбработкаЗавершения) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ПолучитьНастройкиОтправкиЭДО(Организация, Контрагент, ВидДокумента, ОбработкаЗавершения) 
```
</details>

## СохранитьПараметрыПодчиненнойФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура СохранитьПараметрыПодчиненнойФормы(Форма) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.СохранитьПараметрыПодчиненнойФормы(Форма) 
```
</details>

## АдресДоставкиКорректен
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция АдресДоставкиКорректен(АдресЗначение) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиент.АдресДоставкиКорректен(АдресЗначение));
```
</details>

## СохранитьАдресДляПодбора
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура СохранитьАдресДляПодбора(ЗначениеОтбора, Результат) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.СохранитьАдресДляПодбора(ЗначениеОтбора, Результат) 
```
</details>

## СохранитьАдресДляПодбора_Завершение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура СохранитьАдресДляПодбора_Завершение(Результат, ПараметрыФормы) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.СохранитьАдресДляПодбора_Завершение(Результат, ПараметрыФормы) 
```
</details>

## НавигацияПоОшибкамЗаполнения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура НавигацияПоОшибкамЗаполнения(Форма, ОшибкиЗаполненияТаблица, ТекущаяСтрока, Назад = Ложь) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.НавигацияПоОшибкамЗаполнения(Форма, ОшибкиЗаполненияТаблица, ТекущаяСтрока, Назад);
```
</details>

## ПодробнееОбОшибкеЗаполнения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПодробнееОбОшибкеЗаполнения(Форма, ТекущиеДанные, ИдентификаторОтправителя) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ПодробнееОбОшибкеЗаполнения(Форма, ТекущиеДанные, ИдентификаторОтправителя));
```
</details>

## ПоказатьОшибкуЗаполнения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ПоказатьОшибкуЗаполнения(Форма, ТекущиеДанные = Неопределено, ИдентификаторОтправителя = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ПоказатьОшибкуЗаполнения(Форма, ТекущиеДанные, ИдентификаторОтправителя);
```
</details>

## ЗаполнитьТаблицуОшибокЭПД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ЗаполнитьТаблицуОшибокЭПД(СсылкаИлиФорма, ОшибкиЗаполненияТаблица, ОчищатьТаблицу = Истина) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиент.ЗаполнитьТаблицуОшибокЭПД(СсылкаИлиФорма, ОшибкиЗаполненияТаблица, ОчищатьТаблицу);
```
</details>

## ПриОткрытииПодчиненнойФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ПриОткрытииПодчиненнойФормы(Форма) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ПриОткрытииПодчиненнойФормы(Форма) 
```
</details>

## СведенияУказаныКорректно
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция СведенияУказаныКорректно(Форма) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиент.СведенияУказаныКорректно(Форма));
```
</details>

## ПолучитьОтборХранимыхДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ПолучитьОтборХранимыхДанных(КонтейнерРеквизитов, Форма, ГруппаДанных) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиент.ПолучитьОтборХранимыхДанных(КонтейнерРеквизитов, Форма, ГруппаДанных) 
```
</details>

## ПолучитьИмяОбъектаФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьИмяОбъектаФормы(Форма) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиент.ПолучитьИмяОбъектаФормы(Форма));
```
</details>

## ЗапретитьИзменения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЗапретитьИзменения(Форма) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиент.ЗапретитьИзменения(Форма));
```
</details>

## ВывестиФормуВводаДанных
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ВывестиФормуВводаДанных(Форма, Знач ИмяКоманды, ТекущиеДанные = Неопределено, ЭтоВыбор = Истина, ЕстьХранимыеДанные = Ложь, ОповещениеОЗавершении = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ВывестиФормуВводаДанных(Форма, ИмяКоманды, ТекущиеДанные, ЭтоВыбор, ЕстьХранимыеДанные, ОповещениеОЗавершении);
```
</details>

## ОткрытиеФормыПоГиперссылке_Завершение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


//@skip-check module-structure-method-in-regions
Процедура ОткрытиеФормыПоГиперссылке_Завершение(РезультатВыбора, ВходящийКонтекст) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ОткрытиеФормыПоГиперссылке_Завершение(РезультатВыбора, ВходящийКонтекст) 
```
</details>

## ОчиститьПодчиненныеТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ОчиститьПодчиненныеТаблицы(Форма, ИмяТаблицы, ИдентификаторСтрокиРодителя, Отказ) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ОчиститьПодчиненныеТаблицы(Форма, ИмяТаблицы, ИдентификаторСтрокиРодителя, Отказ) 
```
</details>

## АвтоПодборХранимыхДанныхЭПД
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура АвтоПодборХранимыхДанныхЭПД(Элемент, Текст, ДанныеВыбора, Отбор, СтандартнаяОбработка) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.АвтоПодборХранимыхДанныхЭПД(Элемент, Текст, ДанныеВыбора, Отбор, СтандартнаяОбработка) 
```
</details>

## СобратьОписаниеПодчиненныхСтрок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура СобратьОписаниеПодчиненныхСтрок(КонтейнерРеквизитов, МассивИдентификаторов, ПодчиненныеСтроки) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.СобратьОписаниеПодчиненныхСтрок(КонтейнерРеквизитов, МассивИдентификаторов, ПодчиненныеСтроки));
```
</details>

## ЗаполнитьТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ЗаполнитьТаблицы(КонтейнерРеквизитов, Форма, Знач ТаблицыФормы, Знач ИдентификаторСтрокиРодителя = Неопределено, Знач ПрефиксТитула = "", Знач ЭтоДопТитул = Ложь, ГруппаДанных = "") Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ЗаполнитьТаблицы(КонтейнерРеквизитов, Форма, ТаблицыФормы, ИдентификаторСтрокиРодителя, ПрефиксТитула, ЭтоДопТитул, ГруппаДанных);
```
</details>

## ЗаполнитьПодчиненныеТаблицы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ЗаполнитьПодчиненныеТаблицы(ПодчиненныеТаблицы, ИмяТаблицы, ТаблицыФормы, КонтейнерРеквизитов, СтрокаРодитель, ПрефиксТитула, ЭтоДопТитул, МассивЗаполненныхТаблиц) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ЗаполнитьПодчиненныеТаблицы(ПодчиненныеТаблицы, ИмяТаблицы, ТаблицыФормы, КонтейнерРеквизитов, СтрокаРодитель, ПрефиксТитула, ЭтоДопТитул, МассивЗаполненныхТаблиц));
```
</details>

## ТаблицаПриНачалеРедактирования
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ТаблицаПриНачалеРедактирования(Элемент, Форма, НоваяСтрока, Копирование) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ТаблицаПриНачалеРедактирования(Элемент, Форма, НоваяСтрока, Копирование) 
```
</details>

## ВводАдреса
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ВводАдреса(ИмяЭлемента, Форма, ВладелецАдреса = Неопределено, ТекущиеДанные = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ВводАдреса(ИмяЭлемента, Форма, ВладелецАдреса, ТекущиеДанные);
```
</details>

## ВводАдреса_Завершение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ВводАдреса_Завершение(Результат, ДополнительныеПараметры) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ВводАдреса_Завершение(Результат, ДополнительныеПараметры) 
```
</details>

## ЗаполнениеРеквизитовИзОбщегоПоляВвода
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ЗаполнениеРеквизитовИзОбщегоПоляВвода(Элемент, Форма) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ЗаполнениеРеквизитовИзОбщегоПоляВвода(Элемент, Форма) 
```
</details>

## ПолучитьУИДМинтранса
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Процедура ПолучитьУИДМинтранса(ОписаниеОповещения, ИдентификаторАбонента, ЭтоВторойТитул = Ложь, ДокументСсылка = Неопределено) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ПолучитьУИДМинтранса(ОписаниеОповещения, ИдентификаторАбонента, ЭтоВторойТитул, ДокументСсылка);
```
</details>

## ПолучитьУИДМинтранса_ПослеАвторизации
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ПолучитьУИДМинтранса_ПослеАвторизации(РезультатАвторизации, ДополнительныеПараметры) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ПолучитьУИДМинтранса_ПослеАвторизации(РезультатАвторизации, ДополнительныеПараметры) 
```
</details>

## ТребуетсяИдентификацияФайлаТитула
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ТребуетсяИдентификацияФайлаТитула(ДокументСсылка, ТекущийТитул, ВерсииТитулов) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиент.ТребуетсяИдентификацияФайлаТитула(ДокументСсылка, ТекущийТитул, ВерсииТитулов) 
```
</details>

## ИмяОбъектаФормы
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl


Функция ИмяОбъектаФормы(ИмяФормы) Экспорт
```

Пример вызова
```bsl
Результат = ОбменСГИСЭПДКлиент.ИмяОбъектаФормы(ИмяФормы));
```
</details>

## ФормированияДереваСоответствийЗавершение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Оповещает о завершении формирования дерева соответствий.
//
// Параметры:
//  Результат - Структура - Структура с результатом работы фонового задания
//  ДополнительныеПараметры -Структура - Дополнительные параметры
Процедура ФормированияДереваСоответствийЗавершение(Результат, ДополнительныеПараметры) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ФормированияДереваСоответствийЗавершение(Результат, ДополнительныеПараметры) 
```
</details>

## ОповеститьОНеобходимостиОбновитьОформлениеКнопок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Оповещает о необходимости обновить оформление кнопок.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
//  ИдентификаторСтрокиРодителя - Строка, Неопределено - Идентификатор строки родителя
//  ИмяОткрытойПодчиненнойФормы - Строка, Неопределено - Имя открытой подчиненной формы
Процедура ОповеститьОНеобходимостиОбновитьОформлениеКнопок(Форма, Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ОповеститьОНеобходимостиОбновитьОформлениеКнопок(Форма, );
```
</details>

## ОткрытьОписаниеОшибки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Процедура ОткрытьОписаниеОшибки(ОбъектСсылка, ФормаВладелец) Экспорт
```

Пример вызова
```bsl
ОбменСГИСЭПДКлиент.ОткрытьОписаниеОшибки(ОбъектСсылка, ФормаВладелец) 
```
</details>

---

© 2024, **ООО 1С-Софт**  
Все права защищены. Эта программа и сопроводительные материалы предоставляются  
в соответствии с условиями лицензии [Attribution 4.0 International (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/legalcode).

---
