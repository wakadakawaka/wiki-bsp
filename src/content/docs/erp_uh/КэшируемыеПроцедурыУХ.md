---
title: Кэшируемые процедуры УХ
slug: erp-uh/кэшируемые-процедуры-ух
---


## ПолучитьОписаниеТиповПоТипуЗначения
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Возвращает объект типа ОписаниеТипов, соответствующий указанному значению
// типа ПеречислениеСсылка.ТипыЗначенийПоказателейОтчетов.
//
Функция ПолучитьОписаниеТиповПоТипуЗначения(ТипЗначения,Размерность=0,УровеньТочности=0) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТиповПоТипуЗначения(ТипЗначения, Размерность, УровеньТочности);
```
</details>

## ПолучитьПустоеЗначениеПоТипуПоказателя
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПустоеЗначениеПоТипуПоказателя(ТипЗначенияПоказателя) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьПустоеЗначениеПоТипуПоказателя(ТипЗначенияПоказателя) 
```
</details>

## ПолучитьПустоеЗначениеПоВидуАналитики
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПустоеЗначениеПоВидуАналитики(ВидАналитики) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьПустоеЗначениеПоВидуАналитики(ВидАналитики) 
```
</details>

## ПолучитьОписаниеТипаПоТипуПоказателя
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОписаниеТипаПоТипуПоказателя(ТипЗначенияПоказателя) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТипаПоТипуПоказателя(ТипЗначенияПоказателя) 
```
</details>

## ПолучитьПустуюСсылкуПоТаблицеАналитики
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПустуюСсылкуПоТаблицеАналитики(ТаблицаАналитики) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьПустуюСсылкуПоТаблицеАналитики(ТаблицаАналитики) 
```
</details>

## ВернутьМакетПоБланкуОтчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВернутьМакетПоБланкуОтчета(БланкОтчета) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ВернутьМакетПоБланкуОтчета(БланкОтчета) 
```
</details>

## ПолучитьПустоеЗначениеУникальногоИдентификатора
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПустоеЗначениеУникальногоИдентификатора() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьПустоеЗначениеУникальногоИдентификатора() 
```
</details>

## ДлинаКодаСправочника
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ДлинаКодаСправочника(Имя) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ДлинаКодаСправочника(Имя) 
```
</details>

## ПолучитьПараметрыОбъектаДляЗаписи
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПараметрыОбъектаДляЗаписи(ИмяОбъекта, Повторно = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьПараметрыОбъектаДляЗаписи(ИмяОбъекта, Повторно);
```
</details>

## ПолучитьДанныеПараметрическойНастройки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьДанныеПараметрическойНастройки(ПравилоОбработки,ДатаАктуальности) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьДанныеПараметрическойНастройки(ПравилоОбработки, ДатаАктуальности) 
```
</details>

## ПолучитьДанныеПараметрическойНастройкиРасширенный
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьДанныеПараметрическойНастройкиРасширенный(ИсточникПараметрическойНастройки, ТипПараметрическойНастройки, ДатаАктуальности) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьДанныеПараметрическойНастройкиРасширенный(ИсточникПараметрическойНастройки, ТипПараметрическойНастройки, ДатаАктуальности) 
```
</details>

## ПолучитьПравилаПересчетаПоказателей
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПравилаПересчетаПоказателей(ПравилоОбработки,ДатаАктуальности) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьПравилаПересчетаПоказателей(ПравилоОбработки, ДатаАктуальности) 
```
</details>

## ЕстьРеквизитОбъектаВИБ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ЕстьРеквизитОбъектаВИБ(ВИБ,ТаблицаАналитики,ИмяРеквизита) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ЕстьРеквизитОбъектаВИБ(ВИБ, ТаблицаАналитики, ИмяРеквизита) 
```
</details>

## ПолучитьСчетТР
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСчетТР(ВидОтчета) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьСчетТР(ВидОтчета) 
```
</details>

## ПолучитьПоказателиТР
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПоказателиТР(ВидОтчета, СчетТР) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьПоказателиТР(ВидОтчета, СчетТР) 
```
</details>

## ИнициализироватьТаблицуОперандов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИнициализироватьТаблицуОперандов(ЧислоДопАналитик) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ИнициализироватьТаблицуОперандов(ЧислоДопАналитик) 
```
</details>

## ИнициализироватьТаблицуПротокола
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИнициализироватьТаблицуПротокола() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ИнициализироватьТаблицуПротокола() 
```
</details>

## ПолучитьСинонимКлючевогоРеквизита
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьСинонимКлючевогоРеквизита(ИмяРеквизита) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьСинонимКлючевогоРеквизита(ИмяРеквизита) 
```
</details>

## ИспользоватьУправлениеНСИ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ИспользоватьУправлениеНСИ() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ИспользоватьУправлениеНСИ() 
```
</details>

## ПолучитьОписаниеРегистраТекущейИБ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОписаниеРегистраТекущейИБ(ИмяРегистра) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеРегистраТекущейИБ(ИмяРегистра) 
```
</details>

## ПолучитьПустыеЗначенияАналитик
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПустыеЗначенияАналитик() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьПустыеЗначенияАналитик() 
```
</details>

## ПолучитьОписаниеТиповСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

///////////////////////////////////////////////////////////////////////////////////
// Процедуры и функции получения описаний типов значений

// Служебная функция, предназначенная для получения описания типов строки, заданной длины.
//
// Параметры:
//  ДлинаСтроки - число, длина строки.
//
// Возвращаемое значение:
//  Объект "ОписаниеТипов" для строки указанной длины.
//
Функция ПолучитьОписаниеТиповСтроки(ДлинаСтроки) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТиповСтроки(ДлинаСтроки) 
```
</details>

## ПолучитьОписаниеТиповЧисла
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Служебная функция, предназначенная для получения описания типов числа, заданной разрядности.
//
// Параметры:
//  Разрядность 			- число, разряд числа.
//  РазрядностьДробнойЧасти - число, разряд дробной части.
//
// Возвращаемое значение:
//  Объект "ОписаниеТипов" для числа указанной разрядности.
//
Функция ПолучитьОписаниеТиповЧисла(Знач Разрядность, Знач РазрядностьДробнойЧасти=0, Знач ДопустимыйЗнакЧисла = Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТиповЧисла(Разрядность, РазрядностьДробнойЧасти, ДопустимыйЗнакЧисла);
```
</details>

## ПолучитьОписаниеТиповСсылка
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Служебная функция, предназначенная для получения описания типов ссылочного значения
// по переданному строковому описанию
//
// Параметры:
//  ТекстОписание 			- строка
// Возвращаемое значение:
//  Объект "ОписаниеТипов" описанного ссылочного объекта метаданных.
//
Функция ПолучитьОписаниеТиповСсылка(ТекстОписание) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТиповСсылка(ТекстОписание) 
```
</details>

## ПолучитьОписаниеТиповБулево
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Служебная функция, предназначенная для получения описания типов булево.
//
// Возвращаемое значение:
//  Объект "ОписаниеТипов" булева типа.
//
Функция ПолучитьОписаниеТиповБулево() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТиповБулево() 
```
</details>

## ПолучитьОписаниеТиповТЗ
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Служебная функция, предназначенная для получения описания типов таблица значений.
//
// Возвращаемое значение:
//  Объект "ОписаниеТипов" булева типа.
//
Функция ПолучитьОписаниеТиповТЗ() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТиповТЗ() 
```
</details>

## ПолучитьОписаниеТиповСписокЗначений
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Служебная функция, предназначенная для получения описания типов список значений.
//
// Возвращаемое значение:
//  Объект "ОписаниеТипов" булева типа.
//
Функция ПолучитьОписаниеТиповСписокЗначений() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТиповСписокЗначений() 
```
</details>

## ПолучитьОписаниеТиповСтруктура
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Служебная функция, предназначенная для получения описания типов структура.
//
// Возвращаемое значение:
//  Объект "ОписаниеТипов" типа Структура
//
Функция ПолучитьОписаниеТиповСтруктура() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТиповСтруктура() 
```
</details>

## ПримитивныйТипСтрока
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Служебная функция, определяющая примитивность переданного типа
//
Функция ПримитивныйТипСтрока(СтрокаТип) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПримитивныйТипСтрока(СтрокаТип) 
```
</details>

## ПолучитьТипЗначенияПоказателяПоСтроке
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьТипЗначенияПоказателяПоСтроке(СтрокаТип) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьТипЗначенияПоказателяПоСтроке(СтрокаТип) 
```
</details>

## ПолучитьОбъектМетаданныхПоТаблицеАналитики
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОбъектМетаданныхПоТаблицеАналитики(ТаблицаАналитики) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОбъектМетаданныхПоТаблицеАналитики(ТаблицаАналитики) 
```
</details>

## ПолучитьОписаниеПримитивногоТипаИзСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОписаниеПримитивногоТипаИзСтроки(МассивТиповВИБ) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеПримитивногоТипаИзСтроки(МассивТиповВИБ) 
```
</details>

## ПолучитьДанныеТипа
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьДанныеТипа(СтрокаТип) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьДанныеТипа(СтрокаТип) 
```
</details>

## ПолучитьОписаниеТиповИзСтрокиТипов
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОписаниеТиповИзСтрокиТипов(СтрокаТипов) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТиповИзСтрокиТипов(СтрокаТипов) 
```
</details>

## ПреобразоватьТипИзСтроки
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПреобразоватьТипИзСтроки(Тип,ВозвращатьТип=Истина,ТолькоПримитивные=Истина) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПреобразоватьТипИзСтроки(Тип, ВозвращатьТип, ТолькоПримитивные);
```
</details>

## ПолучитьОписаниеТиповРесурсаЗначение
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьОписаниеТиповРесурсаЗначение() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьОписаниеТиповРесурсаЗначение() 
```
</details>

## ПолучитьНомерВерсии
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьНомерВерсии(Версия) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьНомерВерсии(Версия) 
```
</details>

## глОтносительныйПериодПоДате
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

/////////////////////////////////////////////////////////////
/// ПРОЦЕДУРЫ И ФУНКЦИИ РАБОТЫ С ПЕРИОДАМИ

Функция глОтносительныйПериодПоДате(ДатаНачала, Периодичность, Знач Смещение, ТихийРежим = Ложь, Реструктурировать = Истина) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.глОтносительныйПериодПоДате(ДатаНачала, Периодичность, Смещение, ТихийРежим, Реструктурировать);
```
</details>

## ВернутьОтносительнуюДату
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ВернутьОтносительнуюДату(ДатаНачала, Периодичность,Знач Смещение) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ВернутьОтносительнуюДату(ДатаНачала, Периодичность, Смещение) 
```
</details>

## ДатаНачалаПериода
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Функция возвращает ближайшую дату начала периода планирования
//
// Параметры
//	Периодичность (Перечисления.Периодичность)	- периодичность планирования по сценарию.
//	ДатаВПериоде (Дата)							- произвольная дата
//
// Возвращаемое значение:
//	ДатаНачалаПериода		– ближайшая дата начала периода планирования
//
Функция ДатаНачалаПериода(ДатаВПериоде,Периодичность) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ДатаНачалаПериода(ДатаВПериоде, Периодичность) 
```
</details>

## ДатаКонцаПериода
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

// Функция возвращает ближайшую дату окончания периода планирования
//
// Параметры
//	Периодичность (Перечисления.Периодичность)	- периодичность планирования по сценарию.
//	ДатаВПериоде (Дата)							- произвольная дата
//
// Возвращаемое значение:
//	ДатаКонцаПериода	– ближайшая дата окончания периода планирования
//
Функция ДатаКонцаПериода(ДатаВПериоде,Периодичность) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ДатаКонцаПериода(ДатаВПериоде, Периодичность) 
```
</details>

## НачалоНеделиПоМесяцу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НачалоНеделиПоМесяцу(ДатаНачала) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.НачалоНеделиПоМесяцу(ДатаНачала) 
```
</details>

## КонецНеделиПоМесяцу
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция КонецНеделиПоМесяцу(ДатаНачала) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.КонецНеделиПоМесяцу(ДатаНачала) 
```
</details>

## НачалоДекады
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НачалоДекады(ДатаНачала) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.НачалоДекады(ДатаНачала) 
```
</details>

## КонецДекады
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция КонецДекады(ДатаНачала) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.КонецДекады(ДатаНачала) 
```
</details>

## ДатаНачалаПериодаОтчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ДатаНачалаПериодаОтчета(ПериодОтчета) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ДатаНачалаПериодаОтчета(ПериодОтчета) 
```
</details>

## ДатаОкончанияПериодаОтчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ДатаОкончанияПериодаОтчета(ПериодОтчета) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ДатаОкончанияПериодаОтчета(ПериодОтчета) 
```
</details>

## НачалоГодаПериода
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция НачалоГодаПериода(ПериодОтчета) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.НачалоГодаПериода(ПериодОтчета) 
```
</details>

## КонецГодаПериода
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция КонецГодаПериода(ПериодОтчета) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.КонецГодаПериода(ПериодОтчета) 
```
</details>

## ПериодПринадлежитГодуРасчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПериодПринадлежитГодуРасчета(ТекущийПериод,ПериодРасчета) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПериодПринадлежитГодуРасчета(ТекущийПериод, ПериодРасчета) 
```
</details>

## ПолучитьВсеПериоды
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьВсеПериоды(Периодичность, ТекстОтбора="", ПараметрОтбора=Неопределено) Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьВсеПериоды(Периодичность, ТекстОтбора, ПараметрОтбора);
```
</details>

## ПолучитьПериодичностьТрансляцииПроводок
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция ПолучитьПериодичностьТрансляцииПроводок() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.ПолучитьПериодичностьТрансляцииПроводок() 
```
</details>

## МассивРегистровФинансовогоУчета
<details style="margin: 1em 0; padding: 0.5em; border: 1px solid #ccc; border-radius: 6px;">

<summary style="font-weight: bold; cursor: pointer;">Описание, пример вызова</summary>

```bsl

Функция МассивРегистровФинансовогоУчета() Экспорт
```

Пример вызова
```bsl
Результат = КэшируемыеПроцедурыУХ.МассивРегистровФинансовогоУчета() 
```
</details>

---

© 2024, **ООО 1С-Софт**  
Все права защищены. Эта программа и сопроводительные материалы предоставляются  
в соответствии с условиями лицензии [Attribution 4.0 International (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/legalcode).

---
